# Confirmed false positives, verified by manual inspection of the flagged
# lines — not real credentials, just test fixture strings shaped enough like
# a generic API key to trip gitleaks' entropy-based `generic-api-key` rule.
#
# Commit-bound `<sha>:<file>:<rule-id>:<line>` fingerprints only match the
# exact commit that introduced the line. A squash- or rebase-merge of this
# branch gives that commit a new SHA (rebase) or drops it from history
# entirely (squash), so a commit-bound fingerprint can stop matching after
# merge. Confirmed by reproduction that gitleaks also accepts the shorter
# `<file>:<rule-id>:<line>` form (no commit prefix) and that it still
# suppresses the finding at whichever commit introduced it — that form is
# used here so this stays correct across any merge strategy.
packages/storage/src/repositories/operations.test.ts:generic-api-key:213
packages/storage/src/repositories/operations.test.ts:generic-api-key:223
