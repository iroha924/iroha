# iroha — Implementation Decision Log

> Status: Implementation Baseline v1  
> Updated: 2026-07-21

## Confirmed Open Questions

| ID | Decision | Status |
|---|---|---|
| OQ-001 | pnpm 11 workspaces + Turborepo; one lockfile; `workspace:*` | Accepted |
| OQ-002 | Claude Code >=2.1.198; Codex baseline/minimum 0.144.5; feature detection required | Accepted |
| OQ-003 | Tier 1: macOS 14 arm64/x64, Ubuntu 22.04 arm64/x64, Windows 11 x64, WSL2 | Accepted |
| OQ-004 | GitHub first; GitLab provider contract/fixtures only in v0.1 | Accepted |
| OQ-005 | Optional Voyage `voyage-4-large`, 1024 dimensions; FTS-only is zero-config default | Accepted |
| OQ-006 | Checkpoint input fixed by `schemas/checkpoint-v1.schema.json` | Accepted |
| OQ-007 | One human-approved canonical Session Summary per Agent Session; revisions through new approval | Accepted |
| OQ-008 | RRF-based hybrid ranking and 60-query evaluation gate | Accepted |
| OQ-009 | Canonical layout/body/frontmatter fixed by Canonical Data Contract and JSON Schema | Accepted |
| OQ-010 | GitHub Releases + scoped npm + dual repository marketplaces; bundled artifacts and attestations | Accepted |

## Additional decisions

| ID | Decision | Status |
|---|---|---|
| ID-011 | Published npm package is `@iroha-labs/iroha`; binary and plugins remain `iroha` | Accepted |
| ID-012 | ESM-only TypeScript packages and tsdown Node bundles | Accepted |
| ID-013 | No background daemon in v0.1; Hooks use bounded local DB operations | Accepted |
| ID-014 | No remote Embedding or Forge call inside Hooks | Accepted |
| ID-015 | Dashboard auth uses one-time URL fragment exchange and process-lifetime HttpOnly cookie | Accepted |
| ID-016 | No WebSocket/SSE in v0.1; local UI polling only | Accepted |
| ID-017 | React Router + TanStack Query + Tailwind + React Flow + Recharts | Accepted |
| ID-018 | GitHub Forge integration is P1 and may follow the offline vertical slice | Accepted |
| ID-019 | Public license selection is deferred until before first public release; resolved in WP-11b to **Apache-2.0** (product-owner choice; see ID-039) | Accepted |
| ID-020 | Fixed a cross-contract inconsistency found during WP-01: `schemas/canonical-v1.schema.json` used frontmatter `type: review` (and inner object key `review`) for Review Learning documents, while `schemas/checkpoint-v1.schema.json` (`proposal.type`), `migrations/001_initial.sql` (`knowledge_items.knowledge_type`, `candidates.candidate_type`), and `implementation/mcp-contract.md` §7 all used `review_learning`. `entities.entity_type` already reserves `review` for `review_comments` rows (individual PR review comments) separately from `review_learning` for the knowledge item, so canonical frontmatter `type: review` would have collided with that. Canonical `type` is now `review_learning`, the `$defs` key is `reviewLearning`, and the type-specific frontmatter object is `review_learning: { category: ... }`. `rev_` ID prefix and `knowledge/reviews/` path are unchanged. Verified via Draft 2020-12 positive/negative/regression fixtures (`jsonschema` 4.25.1) against both `schemas/` and `docs/product/schemas/` copies. Confirmed with the product owner before applying (pre-implementation, no canonical data exists yet). | Accepted |
| ID-021 | Dropped macOS x64 (`macos-14-large`) from the Tier 1 CI matrix in `.github/workflows/ci.yml`. GitHub-hosted x64 macOS is only available via paid "-large" runners; the job failed to start on this account with "recent account payments have failed or your spending limit needs to be increased." macOS 14 arm64 (`macos-14`) remains covered. `compatibility.md` §6's Tier 1 definition (macOS 14 arm64/x64) is unchanged — this is a CI-workflow gap, not a support-scope change; re-add the matrix entry once billing is configured. Confirmed with the product owner (chose to drop x64 CI coverage for now rather than configure billing or self-host a runner). | Accepted |
| ID-022 | WP-02 (`@iroha/git`) implementation choices for details left open by prose: (1) the HMAC repository salt (32 random bytes, base64url-encoded) is stored as a `repositorySalt` field inside `<git rev-parse --git-path iroha>/local-config.json`, the local-only file design.md §6 already reserves for this purpose; reads/writes preserve unrelated fields so later work packages can add their own keys to the same file. Writes go through a temp-file-then-`rename` for atomicity; a concurrent-first-write race (two processes both seeing no salt and generating different values, last writer wins) is accepted as a rare, self-correcting condition rather than adding cross-process file locking, since no WP in `implementation-plan.md` scopes a locking primitive yet. (2) `sanitizeRemoteUrl` only strips the userinfo component of `scheme://` remote URLs (where a PAT/token can appear as the "username"); SCP-like `user@host:path` remotes are returned unchanged, since that syntax has no password field and treating a single-letter `host` before `:` as an SCP host would otherwise mis-parse Windows drive-letter local paths (`C:\...`, `C:/...`). (3) `toRepoRelativePath` returns POSIX-style (forward-slash) relative paths on every OS, matching Git's own path convention, since `database-schema.md`'s `files.path` column has no stated format. None of these affect canonical data, security boundaries, or the public MCP/CLI surface; recorded here per the decision rule rather than blocking on human input. | Accepted |
| ID-023 | WP-02 (`@iroha/git`) `remote.ts`/`credential-redaction.ts` reached diminishing returns during PR review: after resolving several dozen confirmed-by-reproduction findings (credential leaks via multiple text-boundary shapes, local-path suppression, `--worktree`/`--includes`/`insteadOf`/multi-value config resolution), each fix increasingly targeted narrower Git CLI edge cases rather than remaining security-critical gaps, and one fix round (the `--worktree` fallback) introduced its own regression (exit 128 for `extensions.worktreeConfig` disabled + multiple worktrees) that a later round had to catch — confirming the marginal fixes were adding as much risk as they removed. Explicitly out of scope for WP-02 without a new ADR: `credential.helper`-sourced credentials, `includeIf` conditional-match resolution beyond plain `include.path`, and any further Git config-resolution edge case discovered only through adversarial fuzzing of `remote.<name>.url` rather than a plausible real configuration. `getSanitizedRemoteUrl`/`sanitizeRemoteUrl`/`redactAbsolutePathsInText` remain security-safe for every case fixed so far (fail toward suppression/redaction, never toward exposure) — this decision bounds further *completeness* work, not correctness of what is already implemented. Confirmed with the product owner (chose to stop chasing further PR-review-discovered edge cases rather than open-endedly harden Git config parsing). | Accepted |
| ID-024 | WP-03 (`@iroha/storage`) implementation choices for details left open, or ambiguous, in prose: (1) `database-schema.md` §4 says migration files are named `<four-digit>_<name>.sql`, but the only actual migration file, `migrations/001_initial.sql`, uses a 3-digit prefix, and every other cross-reference in the doc bundle (`design.md`, `README.md`, this file's own ID-020/ID-021 entries, `validation-report.md`) also cites `001_initial.sql`. Rather than picking a side of that prose/filename disagreement, `migrator.ts`'s filename regex (`/^(\d+)_(.+)\.sql$/`) accepts any digit-count numeric prefix, satisfying both readings without contradicting either. (2) §3's "`PRAGMA busy_timeout = 2500`" and "retry SQLITE_BUSY with bounded jitter for at most 2 seconds" read as two independent rules, but confirmed by reproduction: a single blocked `@libsql/client` call under that PRAGMA is a synchronous native call that stalls this process's whole event loop for up to ~2.5s before ever returning control to JS — so if the 2-second app-level retry budget were counted from before that first attempt, it would already be exhausted by the time the first failure is observed. `transaction.ts`'s `withTransaction` therefore starts its 2-second retry-budget clock only after the first SQLITE_BUSY failure, treating the PRAGMA's own wait and the app-level retry budget as sequential rather than overlapping. (3) `database-schema.md` §11 says "Recursive CTE queries must track visited IDs in the path and enforce both depth and edge limits" for `getNeighbors`/`getPath`/`getSubgraph`; `graph-search.ts` implements the latter two as breadth-first search in application code (one `getNeighbors` SQL call per BFS frontier node) rather than a single recursive SQL CTE, since SQLite recursive CTEs do not guarantee BFS expansion order without a hand-built "visited path as string" workaround, and a local libSQL round-trip is cheap enough that a plain, easily-verified BFS was judged simpler and more trustworthy (same required properties — bounded depth, edge limit, cycle avoidance via a visited-entity set — are preserved). (4) Of `database-schema.md` §13's application-level integrity checks, `integrity.ts`'s `checkIntegrity()` implements only the ones expressible purely against this database: approved-knowledge-without-canonical-document, FTS-index/`search_documents` drift (via each FTS5 table's `_docsize` shadow table — confirmed by reproduction that a plain `count(*)`/unfiltered `SELECT` against an external-content FTS5 table always mirrors its content table's row range regardless of actual index sync state, so it cannot detect this drift), stale embedding `content_hash`, and relations pointing to a rejected entity. Checks needing filesystem access to canonical files (path existence, filename/ID agreement) are left to the CLI/sync layer that has that access, and "no canonical ID represented by multiple paths" is skipped as already structurally guaranteed by `canonical_documents.entity_id` being its primary key. None of these affect canonical data, security boundaries, or the public MCP/CLI surface; recorded here per the decision rule rather than blocking on human input. (5) `migrator.ts`'s `getAppliedMigrations` (read) and `db.executeMultiple(file.sql)` (write) are not atomic across OS processes; two processes both calling `runMigrations` against the same not-yet-migrated file at once race on the migration's own `CREATE TABLE`, confirmed by reproduction to surface as a generic `INTERNAL_ERROR` (`SQLITE_ERROR: table ... already exists`, a code `errors.ts` does not classify as `DB_BUSY`) rather than a distinguishable retry signal — though re-running `runMigrations` afterward cleanly reports `ok([])`. This is accepted as a rare, self-correcting race (only reachable during the very first `iroha init` against a given repository, at the exact instant two processes both run it) rather than adding cross-process locking, matching ID-022's identical reasoning for the repository-salt write race: no WP in `implementation-plan.md` scopes a locking primitive yet. (6) `database-schema.md` §13 lists eight application-level integrity checks; `checkIntegrity()` implements four (point 4 above) and explicitly excludes two more (path-existence/filename-ID agreement, deferred to the CLI/sync layer; no-multiple-paths, structurally guaranteed). The remaining one, "every active Guardrail has a valid guard spec," needs no filesystem access and so does not fit either exclusion reason — it is omitted because no guard-spec JSON Schema exists anywhere in the repository yet (`schemas/` has no `guard-spec` definition; `hooks-contract.md`/`mcp-contract.md` reference "a machine-evaluable guard spec" without defining its concrete shape), so "valid" beyond well-formed JSON is not yet checkable; `knowledge_items`'s own `CHECK` constraints already guarantee `guard_spec_json` is present and well-formed JSON when `enforcement = 'guardrail'`. Revisit once a guard-spec schema exists (likely alongside Guardrail-authoring work, not yet scoped to a WP). | Accepted |
| ID-025 | WP-04 (`@iroha/config`, `@iroha/canonical`) implementation choices for details `compatibility.md`/`canonical-schema.md` leave open: (1) library choices, none named anywhere in the doc bundle or pnpm catalog before this WP: `yaml` (eemeli/yaml) for all YAML parsing (frontmatter, `config.yaml`, `labels.yaml`) as the actively-maintained ESM/TypeScript-native standard; `mdast-util-from-markdown`/`mdast-util-to-string`/`unist-util-visit` for §7 body-template heading validation over hand-rolled line/regex scanning — confirmed by reproduction that a naive `#`-prefix regex misidentifies a heading-shaped line inside a fenced code block, which a real CommonMark AST parser correctly ignores; `secretlint`/`@secretlint/node`/`@secretlint/secretlint-rule-preset-recommend` for §11 step 2 / §12 step 3 secret scanning over hand-rolled regex patterns, chosen because it is a maintained, broad-coverage rule set with a programmatic `executeOnContent({content, filePath})` API (confirmed by reproduction) that scans an in-memory string before any file write, not just files on disk — and because `iroha` ships as an npm CLI that must run secret scanning on arbitrary end-user machines without assuming an external binary (e.g. `gitleaks`) is installed, unlike this repo's own CI-only `secrets-scan` job. (2) `@iroha/canonical`'s contribution to §12's "approval transaction" is scoped to steps 3-7 (Zod/body-template/secret validation, write to sibling temp file, flush, atomic rename, fsync parent directory) only — steps 1/2/8/9/10 (per-repository write lock, candidate reload/concurrency-token check, local DB entity/relation/search commit, approval audit record, unlock) need `@iroha/storage`/`@iroha/git`, which `compatibility.md` §4 does not allow `@iroha/canonical` to depend on (`domain`, `config` only). A not-yet-built higher layer (CLI/dashboard-api) composes `writeCanonicalDocument`'s result with those packages to complete the full transaction. (3) `computeCanonicalPath` derives the on-disk path (§3-4: type-specific subdirectory, `sessions/YYYY/MM/` from UTC `created_at`, `<id>.md` basename) directly from the validated document's own `id`/`type`/`created_at`, rather than accepting and validating a caller-supplied path — eliminating an entire class of caller/document path-mismatch bugs instead of adding a separate check for it, since `id` has already passed Zod's strict ULID pattern before it is used to build a path (no `..`-traversal risk per `.claude/rules/path-and-symlink-safety.md`). (4) `parseCanonicalDocument` strictly rejects CRLF line endings and a UTF-8 BOM (§5's format contract) but leniently strips *any* number of leading/trailing blank lines around the body (not exactly one), matching what `serializeCanonicalDocument` always produces while tolerating minor hand-edit whitespace variance that is not a substantive contract violation; the round-trip check in `serializeCanonicalDocument` (§11 steps 9-10) uses structural (`node:util`'s `isDeepStrictEqual`) rather than string/JSON equality, since Zod's re-parsed object is not guaranteed to preserve the exact key insertion order of the hand-ordered candidate even when semantically identical. None of these affect canonical data format, security boundaries, or the public MCP/CLI/dashboard-api surface (not yet built); recorded here per the decision rule rather than blocking on human input. | Accepted |
| ID-026 | WP-05 (`@iroha/core`, `@iroha/cli`, `@iroha/search` offline slice) implementation choices for details left open by prose, plus one library choice made with the product owner: (1) CLI argument parsing uses `gunshi` (kazupon), chosen over `citty` (unjs) and `commander` after the product owner weighed maintenance recency against adoption/stability — confirmed by `npm view`: `citty` had a ~2-year release gap (2024-02 to 2026-01) and is still pre-1.0; `gunshi` self-describes as "modern," has an active weekly release cadence, but is pre-1.0 with comparatively low adoption (~61k weekly downloads vs. `commander`'s ~420M) and was itself released the day before this WP (`gunshi@0.37.1`, flagged by pnpm's `minimumReleaseAgeExclude` supply-chain policy) — the product owner chose `gunshi` anyway, aware of the API-churn risk pre-1.0 carries. `pnpm-workspace.yaml`'s `minimumReleaseAgeExclude` records the exception. (2) `@iroha/core`'s WP-05 surface is deliberately narrow — `initRepository`/`syncCanonicalToDatabase`/`rebuildDatabase`/`runDoctor`/`resolveInitializedRepository` only, not the full "application use cases and transactions" `compatibility.md` §4 describes; Hook/MCP/Checkpoint use cases are WP-06/07's addition to the same package. (3) `@iroha/search`'s WP-05 surface (`searchText`) implements only the unicode/trigram-FTS subset of §9's Reciprocal Rank Fusion formula (`rrf = 1.0/(60+unicodeRank) + 0.9/(60+trigramRank)`, vector term omitted) plus §8's short-query `LIKE` fallback — WP-08 adds the vector term to the same sum rather than replacing it, since §9 states "missing ranks contribute zero" for the three rank sources specifically; the separate authority/scope/graph multiplier table is not rank-based (every entity always has an authority value, so it is never "missing") and is omitted instead because `implementation-plan.md` scopes "RRF ranking and authority/scope/graph boosts" to WP-08 as one deliverable, not because of the "missing ranks" sentence. A real correctness bug surfaced and was fixed while building this: FTS5's `MATCH` operator only recognizes the *literal, unaliased* virtual-table name in a `WHERE` clause (confirmed by reproduction — aliasing the table, `FROM search_fts_unicode f ... WHERE f MATCH ?`, fails with "no such column: f"), and an unquoted caller query is itself parsed as FTS5's own query grammar (`-`, `:`, boolean keywords), so `searchText` wraps every whitespace-split query word in `"..."` (doubling embedded `"`) before building the `MATCH` string. (4) `entities.entity_type` has no `session_summary` value (only `session`) while canonical frontmatter's `type` enum has `session_summary` but no plain `session` — resolved by reading `migrations/001_initial.sql` fully rather than treating this as a schema mismatch to escalate: `agent_sessions.id REFERENCES entities(id) CHECK (id GLOB 'ses_*')` and canonical `session_summary` frontmatter uses the same `ses_` id prefix, confirming a `session_summary` canonical document and its underlying agent-session entity intentionally share one `entities` row (`entity_type = 'session'`); `syncCanonicalToDatabase` maps `frontmatter.type === "session_summary" ? "session" : frontmatter.type` accordingly. (5) All synced canonical documents get `entities.authority = 100` regardless of the canonical frontmatter's own `status` (`approved`/`superseded`/`archived`) — database-schema.md §6 only defines one canonical-sourced tier ("approved canonical: 100") and does not state a lower tier for a still-committed-but-superseded document; recalculating authority by lifecycle stage is left for a future WP if the gap proves load-bearing. (6) `canonical_documents.approved_by_actor_id` is left `NULL` by `syncCanonicalToDatabase` — resolving/deduplicating an `actors` row from a canonical document's `approved_by`/`created_by` `actorRef` (which has no external ID when unset) is deferred, since attempting it now without a dedup key would mint a fresh, never-reused `actors` row on every sync of the same document; the field is optional in `UpsertCanonicalDocumentInput` for exactly this reason, and this is expected to be resolved once an actor-identity WP (Checkpoint/MCP flows, WP-07) needs the same resolution. (7) `iroha init`'s doc-scan initially ran unconditionally and covered only `AGENTS.md`/`CLAUDE.md`, following requirements.md Scenario A's plain-language description; a self-review pass then found this contradicted canonical-schema.md §14 verbatim ("`iroha init --scan` creates local candidates from `CLAUDE.md`, `AGENTS.md`, `.claude/rules/**/*.md`, and user-selected docs. It does not copy them into `.iroha/` automatically" plus a required-fields list: source path, content hash, line range, import timestamp, detected scope, link back to the source). Per `investigate-before-asking.md`, this prose/prose conflict was surfaced to the product owner rather than silently resolved either way; the product owner chose full §14 compliance. The corrected implementation (`docs-scan.ts`): scanning only happens behind `iroha init --scan` (`InitRepositoryOptions.scan`, default off); it additionally walks `.claude/rules/**/*.md` recursively; each `candidate_type: "rule"` row's `payload_json` now holds `{title, body, source: {type: "document", path, content_hash}, imported_at, line_range: {start, end}, detected_scope: {paths}}` — `line_range` always spans the whole file (each doc becomes one candidate, not sub-document extracts, so "when stable" in §14 always holds), and `detected_scope.paths` is read from a `.claude/rules/*.md` file's own optional `paths:` YAML frontmatter (the same field this repository's own rules use for path-scoped auto-loading) or defaults to `[]` for `AGENTS.md`/`CLAUDE.md`/a rules file without frontmatter. §14's "user-selected docs" source is not implemented — it needs an interactive doc-picker this non-interactive CLI flag does not have; deferred to whichever future WP adds an interactive or dashboard-driven scan flow. Re-scanning the same unchanged file content is a no-op via a `local_settings` key (`docs_scan:<relative-path>`) storing the last-seen content hash — this state is local-only and is intentionally not reconstructed by `rebuildDatabase` (which only imports canonical, not local, state), so a rebuild causes the next `iroha init --scan` to re-surface a docs-scan candidate even for unchanged file content; this is treated as an acceptable, self-correcting consequence of rebuild's scope rather than a bug. (8) A deleted canonical file's entity is marked `entities.status = "tombstoned"` — a new value with no format constraint at the DB level (`entities.status` has no `CHECK` list, unlike most other status-shaped columns), chosen over reusing the canonical frontmatter status enum (`approved`/`superseded`/`archived`, none of which mean "the file is gone") to keep "deleted from `.iroha/`" distinguishable from "the file's own lifecycle status," matching §13's "Git deletion is imported as a local tombstone" language. (9) `repositories.root_fingerprint` (`NOT NULL UNIQUE`, no format `CHECK`) is computed as `sha256:<hex>` over the resolved Git common-dir path (`computeRootFingerprint`, shared by `initRepository` and `rebuildDatabase`) — distinct from `repositories.id`, which is the *shared* identity read from/written to the git-tracked `.iroha/config.yaml` (canonical-schema.md §9: "generated once and committed"); `root_fingerprint` is local-clone-scoped and not specified anywhere in the doc bundle, so this is recorded as this WP's own choice. (10) `rebuildDatabase` does not implement §12 step 1 ("acquire the repository rebuild lock") — no locking primitive exists anywhere in this codebase yet (no WP in `implementation-plan.md` scopes one), matching the identical reasoning already accepted for ID-022's repository-salt write race and ID-024(5)'s migration-insert race: `.iroha`'s `locks/` local-state directory is created by `initRepository` but nothing writes to it yet. Unlike those two, this is not fully self-correcting — two `iroha sync --rebuild` invocations racing the final `replaceDatabaseAtomically` swap is unguarded — so this gap should be closed before, or as part of, whichever WP first introduces cross-process locking, rather than left indefinitely. (11) `syncCanonicalToDatabase`'s relation import (`insertRelationsForDocument`) is insert-only: a relation present in a document's *previous* revision but removed in a later one is never pruned from the `relations` table (no `DELETE FROM relations` exists anywhere in `@iroha/storage`), so a stale edge from a superseded revision survives indefinitely until the next `iroha sync --rebuild`. Adding relation pruning needs a new storage primitive (a `deleteRelation`-equivalent, keyed on `(from_entity_id, relation_type, to_entity_id, source_kind)` or similar) that does not exist yet; deferred rather than added under review-driven time pressure, since the gap is self-correcting via rebuild and no WP-05 acceptance criterion (Scenario A/C/E) exercises relation removal specifically. None of (2)-(6) and (8)-(11) affect canonical data format, security boundaries, or the public MCP/dashboard-api surface (not yet built); (1) and (7) were confirmed with the product owner directly, since (1) is a durable cross-cutting dependency choice and (7) resolves a genuine prose/prose specification conflict rather than being reversible-behind-a-port. (12) Windows CI reproduction of `replaceDatabaseAtomically` (`@iroha/storage`'s `renameWithRetry`) surfaced a native libSQL file-handle teardown lag that, at least once, exceeded a 9-attempt-scaled and even a fixed-500ms/40-attempt (~20s) retry budget — escalating this budget round after round to chase 100% CI reliability was reconsidered as over-engineering a guarantee the product does not need: §12 does not require any bound on failure-path cleanup timing, and the `.iroha` local database (including any sibling rebuild artifact, each uniquely suffixed by `createSiblingDatabasePath`) is explicitly a disposable, rebuildable index, not canonical data. The final design keeps `renameWithRetry` (the one rename genuinely on the product-required path, §12 step 11) at a modest, real retry — 10 attempts, fixed 300ms delay, ~3s worst case — surfacing a real retryable error if the lock does not clear in that window rather than blocking indefinitely; `rebuildDatabase`'s `removeSiblingDatabase` (failure-path hygiene only, not spec-required at all) was reverted to a single best-effort attempt with no retry. The corresponding test (`packages/core/src/rebuild-database.test.ts`, "fails without replacing the primary database when the final atomic swap fails") no longer asserts the orphaned sibling file is gone, only that the rebuild itself reports failure without touching the primary DB. (13) The EBUSY lag in (12) recurred even at the reduced 3s/1.5s budgets, so root cause was researched rather than re-escalating the retry budget again: SQLite's own docs (https://sqlite.org/tempfiles.html) and matching reports against other Node SQLite bindings on Windows (https://github.com/oven-sh/bun/issues/25964, https://github.com/JoshuaWise/better-sqlite3/issues/376) confirm that in WAL mode, closing the last connection to a database makes SQLite acquire an exclusive lock, checkpoint, and delete the `-wal`/`-shm` files before releasing it, and that this can leave the main `.db` file locked on Windows well after `close()` returns to JS — a known, cross-binding SQLite/Windows limitation, not specific to this codebase's own retry logic. `@iroha/storage`'s `closeDatabase` (`connection.ts`) now runs `PRAGMA journal_mode = DELETE` immediately before `db.close()`, best-effort (it can fail if another connection is still attached, without failing `closeDatabase` itself), so the exclusive-lock-checkpoint-delete sequence is avoided outright rather than raced against; every connection this package opens re-applies `journal_mode = WAL` from `INIT_PRAGMAS` on its next `openDatabase` call, so this is a transient, connection-local switch, not a lasting behavior change. This made `closeDatabase` `async`, and every call site across `@iroha/core`/`@iroha/storage`/their tests (~20 files) was updated to `await` it. The existing `renameWithRetry`/rm-retry budgets from (12) are kept as defense-in-depth, not removed, since the DELETE-mode switch is not confirmed by any of the cited sources to fully eliminate the lock in every case (the bun issue's reporter did not test it, and cross-connection contention can still prevent the mode switch itself from taking effect) — this still needs a Windows CI run to confirm the actual effect. (14) The `journal_mode = DELETE`-before-close fix in (13) was pushed and did **not** resolve the failures — the next Windows CI run reproduced the identical three `EBUSY` failures at the identical call sites, confirming (rather than merely suggesting, per the sources cited in (13)) that this specific Windows/SQLite/WAL file-lock-after-close behavior is not reliably fixable from application-level code: neither retry-budget escalation (12) nor the principled root-cause mitigation (13) made Windows CI consistently green across seven consecutive runs. Continuing to iterate on this from inside the codebase was judged to have exhausted its reasonable return, so the product owner decided to drop Windows from CI rather than keep chasing it: `windows-2025` is removed from `.github/workflows/ci.yml`'s `verify` matrix, and `compatibility.md` §6 moves Windows from Tier 1 (release-blocking) to Tier 2 (best effort), reasoning that the expected user base skews toward macOS. The production-code robustness work from (12)/(13) (`renameWithRetry`, `closeDatabase`'s `journal_mode = DELETE` switch, the modest retry budgets) is kept as-is rather than reverted — it still benefits any Windows user running this outside CI, even though it can no longer be verified by CI — and `windows-ci-compat.md`'s documented patterns remain valid reference material if Windows CI coverage is ever revisited. | Accepted |
| ID-027 | WP-06 (`@iroha/platform`, `@iroha/adapter-claude`, `@iroha/adapter-codex`, `@iroha/core`, `@iroha/storage`) — session token storage location, a prose-vs-machine-contract gap resolved with the product owner. `design.md` §9 and `mcp-contract.md` §5 both state the SessionStart-issued 256-bit session token's HMAC-SHA-256 digest is "stored in the DB," but `migrations/001_initial.sql` and `database-schema.md` §5's table list had **no table or column** for it (a `grep` for token/hmac in the migration found only the `prompt_digest`/`input_digest`/`response_digest` columns and candidates' `revision_token`). Per `CLAUDE.md` ("when prose and a machine-readable contract disagree, stop and report the conflict") and the decision rule (a DB-schema/distribution change needs human input), this was surfaced rather than silently resolved. The product owner chose a **dedicated table via a new forward-only migration** over the two alternatives (a `local_settings` KV entry — no migration but mixes a security token into a general settings store; or deferring token issuance entirely to WP-07, which would leave SessionStart's contract output incomplete). `migrations/002_session_tokens.sql` (mirrored to `docs/product/migrations/`) adds `session_tokens` (`token_hmac` PRIMARY KEY with a `GLOB 'hmac-sha256:*'` CHECK, `repository_id`/`session_id`/`run_id` FKs with `ON DELETE CASCADE`, `platform` CHECK, `issued_at`/`last_used_at`/`expires_at`, plus indexes on `run_id` and `expires_at`), sets `PRAGMA user_version = 2`, and is self-contained (`BEGIN IMMEDIATE`/`COMMIT`) exactly like 001. Only the salt-keyed HMAC of the token is stored, never the plaintext, so a leaked DB yields no usable token. The table is disposable local operational state: `rebuildDatabase` imports canonical only, so `session_tokens` is empty after a rebuild — which is correct, since tokens are per-Run ephemera. `@iroha/storage` gains `insertSessionToken`/`getSessionToken` (the write + read-back surface WP-06 needs); token verification, `last_used_at` bump, and run-completion/expiry invalidation are WP-07's (the MCP server is the only token consumer) and are intentionally not built now (YAGNI). `database-schema.md` §5, `design.md` §7's table group, and `migrator.test.ts` (its "applies the real migration" assertions, and the synthetic-migration crash-recovery/downgrade tests that had used version 2 as a stand-in "unknown future version" — now shifted to version 3 to avoid colliding with the real 002) were all updated in the same change, per the change protocol. | Accepted |
| ID-028 | WP-06 (`@iroha/platform`, `@iroha/adapter-claude`, `@iroha/adapter-codex`, `@iroha/core`) implementation choices for details left open by prose, and scope boundaries for capabilities that structurally belong to later work packages. **Architecture**: (a) adapters may depend only on `@iroha/platform` (compatibility.md §4, biome-enforced), so platform re-exports the `@iroha/domain` primitives adapters need (`NormalizedEvent`/`normalizedEventSchema`, `Result`/`IrohaError`); (b) the repository-keyed HMAC digest, event id, and timestamp an adapter needs to finalize a normalized event are injected as a `NormalizationContext` port, because the HMAC salt lives in `@iroha/git` which adapters may not depend on — the salt never leaves `@iroha/core`/`@iroha/git`; (c) the hook dispatcher (`dispatchHookEvent`), `runHook` orchestration, and `runHookEntry`/`main` entrypoint live in `@iroha/core`, the only package `compatibility.md` §4 lets depend on both adapters and storage. WP-11 bundles `main` into the plugin's `dist/hook.mjs`. **Tool-target handling**: (d) commands are classified via a shared `classifyCommandTarget` (in `@iroha/platform`, so the two adapter extractors cannot drift) that takes the leading token, reduces a program *path* to its basename, and collapses anything that is not a bare program name — notably a `VAR=secret cmd` env-assignment prefix or an absolute path — to the generic label `command`; the full `tool_input` survives only as an `inputDigest`. This closed a HIGH finding from the WP-06 security self-review: the earlier leading-token-only classification (`command.split(/\s+/)[0]`) would have stored a value like `GITHUB_TOKEN=<secret>` or `/Users/alice/bin/deploy.sh` verbatim into `tool_events.target_summary`, violating hooks-contract.md §8/§10 (commands are classified, not stored verbatim; secrets/credentials/absolute paths are never persisted). (e) Codex `apply_patch` edits extract touched file paths from the patch's `*** Add/Update/Delete File:` header lines only — never the diff body — so cross-platform Guardrail parity has the paths without storing patch content; the header regex is matched against the untrimmed, column-0 line so a `+`/`-`/space-prefixed body line that merely contains header-shaped text is not a false-positive target (also from the self-review); (f) `file`/`path` targets are resolved to POSIX repo-relative form via `@iroha/git`'s existing symlink-safe `toRepoRelativePath`, and a target escaping the repository is dropped rather than persisted with an absolute path; (g) the `tool_events` row (single `target_kind`/`target_summary`) stores the first resolved target as a representative — the full `targets` array is used only in memory (per-file relation creation from all targets is deferred, no WP-06 acceptance exercises it); (h) `platform`'s `ToolTarget.operation` was widened to include `| undefined` to structurally match the Zod-inferred normalized target under `exactOptionalPropertyTypes`. **New storage surface**: (i) `getLatestTurnForRun` was added to `@iroha/storage` (the "current Turn" a tool event or Stop refers to) — a small, WP-06-owned lifecycle query. **Checkpoint-required heuristic**: (j) PostToolUse marks the Turn `checkpoint_state = pending` on any file write/delete or command execution — a deliberate over-approximation (better to over-remind than miss a build/test), since fine-grained command classification is not available; Stop then requests a checkpoint exactly once when the Turn is `pending` and `stop_hook_active` is false. **Scope boundaries deferred to later work** (seams present, behavior intentionally minimal): (k) SessionStart context carries the session token, IDs, and a recent checkpoint, but **not** an "Applicable approved knowledge" section — approved-knowledge retrieval (relevance ranking, scope matching) is WP-08's search layer, which the hook does not run; (l) PreToolUse's Guardrail-evaluation seam matches nothing yet because no machine guard-spec JSON Schema exists in the repository (ID-024(6)) — the deny *output* format is implemented and tested on both platforms, but a real match awaits a guard-spec schema (a future Guardrail-authoring WP); (m) `dirty_marker` creation on compact/Stop-with-pending-checkpoint is deferred because no existing `DIRTY_MARKER_TYPES` value denotes "a Turn lacks its checkpoint" — the one-time continuation logic itself is fully implemented; (n) session-token *verification*, `last_used_at` bump, and run-completion/expiry invalidation are WP-07's (the MCP server is the token's only consumer), so `@iroha/core`'s `issueSessionToken` and `@iroha/storage`'s `insertSessionToken`/`getSessionToken` are the only token surface built now. **Verification**: (o) the hooks-contract.md §11 fixture corpus is implemented as tests inside `@iroha/core` (`hook-entry.test.ts` and the adapter tests) rather than a dedicated cross-package `tests/contracts/` harness with a wired `test:contracts` turbo task — that harness remains deferred (consistent with typescript-conventions.md's note that `test:contracts` is not yet implemented in any package); CI end-to-end smoke sessions against real Claude/Codex are out of scope (no real agent in CI), matching the static-fixture approach. (p) The full-project self-review (`/iroha-review`: fresh-context security/spec-compliance/adversarial reviewers + a finding-validator reproduction) surfaced four defects that were **fixed in this WP** rather than deferred: a **HIGH** where neither the hook nor a plain `iroha sync` applied pending migrations, so a DB left at an older `user_version` by an earlier build silently lost all hook function (`INSERT INTO session_tokens` → "no such table" → fail-open, `sync` returning success without repairing) — `runSync` (non-rebuild) now runs `runMigrations` before syncing, per database-schema.md §3 ("init/sync/doctor --repair migrate; hooks never do"); Stop never completed the Turn (`handleStop` now calls `closeTurn` `active → completed` in the non-continuation branches, hooks-contract.md §6.6 steps 1/2/4, so Turns no longer stay `active` forever); the Claude adapter rejected an explicit JSON `null` on optional fields, dropping the whole event (`model`/`permission_mode`/`prompt_id`/`last_assistant_message`/`compact_summary` now use `.nullish()` and fold `null` to absent, matching the Codex adapter's existing `.nullable()`); and relative tool-target paths were resolved against the repo root instead of the agent's cwd, mis-naming files under a subdirectory launch (`resolveTargets` now takes `cwd` and joins a relative value to it by plain string concatenation before the symlink-safe `toRepoRelativePath`, preserving the path-safety invariant). (q) Two review observations are **recorded as accepted** rather than fixed: concurrent SessionStart for one session can leave two `active` runs, because `handleSessionStart` ignores `closeSessionRun`'s CONFLICT result and creates a new run unconditionally — accepted as a rare, low-probability race matching ID-022/ID-024(5)/ID-026(10)'s reasoning (no cross-process locking primitive is scoped to any WP yet), to be closed by whichever WP introduces one; and these hooks-contract items are deferred with no canonical-data/API impact, recorded here for traceability — §6.1 step 6's changed-file-only sync at SessionStart (deferred alongside the WP-08 approved-knowledge retrieval it would feed, since no canonical-consuming output exists yet), the WP-06 "timeout tests" deliverable (no time-budget/DB-busy test ships in this WP; not an acceptance criterion), and §6.5 compaction-trigger recording / §2 step 9 local `event_log` diagnostics (no handler writes `event_log` yet — the privacy-safe direction). Every internal hook failure is fail-open (returns no output, never blocks the agent); no transcript is parsed anywhere in `@iroha/core`. None of these affect canonical data format or the public MCP/dashboard-api surface (not yet built); (b)/(c)/(h)/(i) are reversible-behind-a-port structural choices, and (k)-(o) are plan-consistent scope deferrals, so they are recorded here per the decision rule rather than escalated. | Accepted |
| ID-029 | Consolidated the machine-contract copies to a single source of truth at the repository root. `schemas/*.json` and `migrations/*.sql` had byte-identical duplicates both at repo root and under `docs/product/`, hand-maintained (ID-020 and ID-027 each had to edit both copies) with no `test:contracts` guard yet, so the two copies could silently drift. The runtime reads only the repo-root copies (`packages/storage/src/test-helpers/tmp-db.ts` resolves `../../../../migrations`; nothing in `packages/`, CI, or config reads `docs/product/schemas`/`docs/product/migrations`), so the `docs/product/` copies were removed and repo-root `schemas/` + `migrations/` are now the sole machine contracts. References were updated in the same change: `docs/product/README.md` Document map links (now `../../schemas/`, `../../migrations/001_initial.sql`), `AGENTS.md`, and `.claude/rules/typescript-conventions.md`. The Zod schemas still mirror `schemas/*.schema.json` (now only at repo root) — two representations (Zod `.ts` + JSON Schema) to keep synced, no longer two JSON copies. Historical ID-020/ID-027 wording about updating "both copies" is left as an accurate record of the time. No canonical-data, schema-content, public-API, or distribution change: the machine contracts are byte-unchanged, only their on-disk location was deduplicated; recorded here per the decision rule. | Accepted |
| ID-030 | WP-07 (`@iroha/mcp`, `@iroha/core` mcp/*, `@iroha/storage`, `@iroha/domain`) — the agent-facing stdio MCP server and Checkpoint loop. Implementation choices and scope boundaries for details left open, plus fixes from a fresh-context self-review. **Transport**: (a) tool listing and `tools/call` are handled with the low-level `Server` + manual `ListTools`/`CallTool` handlers, **not** `McpServer.registerTool` — confirmed by reading the SDK 1.29 source that `registerTool` wraps a raw shape in a strip-mode object (silently drops unknown fields) and turns a validation failure into an untyped `McpError`, so honoring mcp-contract.md §4 (every outcome is a typed envelope) and §8 (unknown fields rejected, 256 KiB cap) requires validating input in the dispatcher with a strict Zod schema and building the envelope directly. (b) `compatibility.md` §4 lets `@iroha/mcp` depend only on `@iroha/core`, so a `mcp/facade.ts` re-exports the `@iroha/domain`/`@iroha/storage` primitives the transport needs (Result/IrohaError/Clock/RandomSource/checkpointInputSchema/proposalSchema/ENTITY_TYPES/RELATION_TYPES) — same pattern as ID-028(a). **Session token** (§5): verify recomputes the salt-keyed HMAC, looks it up, checks repository binding + idle expiry + run-active, slides `last_used_at`/`expires_at` on success (new `updateSessionTokenLastUsed`), and returns opaque `INVALID_SESSION_TOKEN`/`SESSION_EXPIRED` with no leak of which check failed or the token/HMAC. **Idempotency** (§9): a shared `runIdempotentWrite` does pre-check → transaction(work + insert idempotency row last) → on `CONFLICT` refetch (a refetch-hit returns the stored result, a refetch-miss returns the original error, so a non-idempotency constraint failure is not masked). `create_checkpoint`'s `checkpointId` (a `chk_` entity) is stored as `result_entity_id`; `propose_knowledge`/`link_entities` omit it because a candidate/relation id is not an `entities` row. **Secret redaction** (§6.6 step 2 / §8): a Checkpoint is a local candidate, so — unlike a canonical write, which rejects on a finding — a flagged free-text field is redacted wholesale and reported in `redactions[]`. The self-review found the initial scan covered only prose fields while `guard.denyCommands`/`guard.tools`/`scope.symbols`/`implementation[].symbol`/`references[].ref`+`url`/`sources[].ref`+`url` were persisted verbatim (a `denyCommands` entry is exactly where an inline-token command would live; a `url` can carry userinfo credentials); redaction was extended to all of them, with a `url`-shaped placeholder so a redacted reference still satisfies `z.url()` at approval time. **Get_relations truncation fix**: the self-review (reproduced) found `truncated` was effectively always `false` because `getNeighbors`/`getSubgraph` cap at exactly their `LIMIT` with no has-more signal; the tools now over-fetch `maxEdges + 1` to detect an overflow. **Link_entities phantom-id fix**: `insertRelation` is `ON CONFLICT DO NOTHING`, so a tuple already present under a different key left the tool returning a never-stored id; it now reads back the real id via a new `getRelationByTuple`. **Deferred to WP-08 / a future WP, surfaced via tool `warnings` (never silently dropped)**: vector search + RRF + authority/scope/graph boosts, provenance (`sources`) and relation enrichment in `search`, `includeBody`, the label/path/symbol/issueRef/date `search` filters, full context-pack ordering + `get_context` scope inputs, `search` `mode=graph`, guardrail *evaluation* (no machine guard-spec JSON Schema exists — ID-024(6)/ID-028(l)), `get_active_rules` `severity` (no `knowledge_items.severity` column; it exists only on the canonical Rule), `create_checkpoint` §6.6 step 6 reference-relation materialization (references are stored on the checkpoint but not yet edges), `propose_knowledge` §6.7 duplicate detection and `supersedesCandidateId` linkage, and `toolName`/`commandCategory` rule matching. **Accepted lower-severity behaviors** (self-review, not fixed): `search`/`get_active_rules` accept a `repositoryPath` and resolve whatever iroha repo it names — no server-worktree containment check — accepted under the single-user local threat model (the resolution stays within a real repo boundary via `safeRealpath`; the agent can already read those local DB files), rather than the §8 server-bound reading; `get_context`'s `maxCharacters` counts only `summary` length (approximate cap); and a non-idempotency `CONFLICT` surfaced during a write (e.g. a Turn deleted mid-flight by a concurrent rebuild) is returned as a generic constraint error. **SCHEMA_MISMATCH** (§4/§11) is surfaced by the existing `assertSupportedSchemaVersion` in `resolveInitializedRepository`; no separate DB `user_version` gate was added (the server assumes a migrated DB and never migrates, matching the hook path). No repository write mutex (§9) is implemented — no cross-process locking primitive is scoped to any WP yet (matching ID-022/ID-024(5)/ID-026(10)/ID-028(q)); the idempotency-rollback design bounds duplicate writes. None of these change canonical data format or the public MCP wire shape beyond what mcp-contract.md already specifies; recorded here per the decision rule. | Accepted |
| ID-031 | Voyage embedding model changed from `voyage-4` to `voyage-4-large` at the product owner's direction (2026-07-20, during WP-08). Verified against Voyage's official docs (docs.voyageai.com/docs/embeddings): `voyage-4-large` is the current recommended general-purpose multilingual retrieval model, 32k context, with `output_dimension` defaulting to 1024 (also 256/512/2048) — so the fixed 1024-dimension vector index and the `F32_BLOB(1024)` schema are unchanged; only the model string differs, and the request/response shape (`POST /v1/embeddings`, `input_type`, `output_dimension`, `Authorization: Bearer`) is unaffected. Updated consistently across `config.search.embedding.model` (`z.literal`), `migrations/001_initial.sql`'s `embeddings_1024` `CHECK (model = ...)`, the `@iroha/storage`/`@iroha/core` model constants, the provider default, and the OQ-005 prose in background/research/design/requirements/compatibility/canonical-schema. **`migrations/001_initial.sql` was edited in place rather than adding a new migration**: v0.1 is unreleased (no deployed databases), SQLite cannot `ALTER` a `CHECK` constraint without a full table rebuild, and the rebuild tests re-apply every migration from empty — so an in-place literal change is correct and simplest here. This supersedes the model choice in OQ-005; the FTS-only-by-default / optional-embedding posture is unchanged. | Accepted |
| ID-032 | WP-08 (`@iroha/search`, `@iroha/core`, `@iroha/storage`, `@iroha/mcp`) — hybrid search, ranking, enrichment, context-pack, the SessionStart knowledge section, and the search-evaluation gate. **Embedding pipeline**: index-time enqueue into `embedding_jobs` (offline-safe, no provider call in the hook — ID-014); a `runEmbeddingSync` worker drains the queue during `iroha sync` (disabled/missing-key → no-op leaving jobs pending; a retryable outage backs off one job and stops the run rather than burning the queue's retry budget; non-retryable → dead-letter + an `embedding_retry` dirty marker); a content-hash guard skips the provider when a current vector already exists; `sync --rebuild` reuses vectors from the previous DB by content hash (best-effort/offline; the previous DB is opened only if it already exists, so a rebuild triggered *because* the DB is gone does not resurrect it). **Voyage provider** lives in `@iroha/search` (§4's "vector provider"); `@iroha/core` injects the `api_key_env`-resolved key (search cannot import config); the key never appears in any error/log/`cause`; the response is Zod-validated at the boundary and reordered by `data[].index`. **Ranking**: `searchHybrid` returns the full fused candidate set (unicode/trigram/vector RRF, `k=60`, weights 1.0/0.9/1.1, "missing ranks contribute zero") scored by authority (×1.25 @100, ×1.10 @80–99) and a bounded recency tie-breaker (`×(1 + 0.05·2^(-ageDays/180))`, ≤5%); `@iroha/core`'s `rankCandidates` then applies the scope/graph boosts (same-symbol ×1.35, same-path ×1.25 via `pathMatches` prefix globbing, same-active-issue ×1.30, graph distance ×1.15/1.08/1.03 by BFS over `getSubgraph` from scope/issue anchors), the hard filters, the final cap, and the enrichment — scope (canonical frontmatter), relations, and work-items are core-owned data. **Filters vs scope**: `paths`/`symbols`/`issueRefs` are scope hints (boost, never exclude); `statuses`/`entityTypes`/`labels`/`from`/`to`/`minimumAuthority` are hard filters. **`mode=graph`**: hybrid (lexical+vector) seeds expanded along the relation graph (`graphExpandedCandidates`, decayed base score per hop). The first cut seeded graph lexically-only, which returned zero for long/cross-lingual relationship queries — fixed to seed with the query vector in both `mcpSearch` and the eval. **Response types** `SourceRef { type, ref, path?, lineStart? }` (aligned to canonical-schema §5 source kinds) and `RelationPreview { relationType, direction, entityId, title }` are defined in `@iroha/core`; they are MCP *output* shapes (constructed, never parsed at a boundary), so they get no `schemas/*.json` mirror — documented in `mcp-contract.md` instead. **Lexical operator routing** (informed by a primary-source hybrid-search review — Voyage docs, Cormack 2009 RRF, SQLite FTS5 docs, BEIR/MTEB, Weaviate/Qdrant/OpenSearch): FTS5's implicit multi-word AND drops to ~0 recall for long NL and cross-lingual queries (the industry-standard lexical arm is OR+BM25), so `buildMatchQuery` routes identifier/path queries to AND (precision) and natural-language queries to OR (recall); every word stays a quoted phrase, so the injection-safety invariant holds; cross-lingual recall still comes only from the vector arm. Kept unchanged as confirmed-current best practice: RRF `k=60` + weights (not re-tuned on 60 queries — overfitting), Voyage `input_type` query/document + 1024-dim + float, no self-normalization (Voyage vectors are unit-length), no query translation (KISS + the no-external-LLM invariant). **Evaluation gate** (database-schema §14): a deterministic offline harness over a synthetic 30-doc / 60-query fixture (20 JA-NL / 15 EN-NL / 15 code / 10 relationship) with per-class + rule-recall metrics; corpus and query vectors are recorded ONCE with a real Voyage key (`record-embeddings.test.ts`, gated by `IROHA_RECORD_EMBEDDINGS=1`) into a committed `embeddings.recorded.json` and replayed, so `pnpm test`/CI stay fully offline. Achieved Recall@10=1.00 / nDCG@10=0.999 / MRR@10=1.00 / approved-Rule Recall@10=1.00, exceeding the 0.85/0.70/0.70/1.00 thresholds. Caveat (from the review): at 30 docs a top-10 return is 1/3 of the corpus, so Recall@10 is structurally inflated and the absolute thresholds are a "floor" — future work should add distractor documents, a baseline-Δ regression gate, and graded (0–3) relevance; recorded, not changed now, because these are the spec-mandated thresholds and the per-class breakdown is the real signal. **ID-030 deferrals now implemented**: vector search + RRF + authority/scope/graph boosts, `sources`/`relations`/`whyRelevant` enrichment, `includeBody`, the label/path/symbol/issueRef/date filters, `get_context` scope inputs, `mode=graph`, and the SessionStart "Applicable approved knowledge" section (approved Rules only — a direct lexical listing, no remote embedding). Still deferred (unchanged): guardrail *evaluation* (no guard-spec schema), `get_active_rules.severity` (no column), cross-process locking. | Accepted |
| ID-033 | WP-09 (`@iroha/core` dashboard use cases, `@iroha/api`, `@iroha/cli`, `apps/dashboard`) — the human control plane: local API, auth, and the React dashboard. **Approval transaction** (canonical-schema.md §12 / design.md §10 / ID-025(2)): `@iroha/canonical.writeCanonicalDocument` owns steps 3–7 only, so the composed transaction lives in a new `@iroha/core.approveCandidate` (the "higher layer" ID-025(2) named) — `@iroha/api` may depend only on `@iroha/core` (compatibility.md §4), so every data-access use case is added to core, not the API. Fixed order: in-process per-repository write lock → reload candidate + verify optimistic `revision_token` → `writeCanonicalDocument` (Zod + body-template + secret scan + atomic write; a detected secret or invalid body fails BEFORE any DB change, so "secret warning blocks approval" holds) → one DB transaction that imports the just-written file via the **same `importCanonicalDocument` path `sync --rebuild` uses** (exported for reuse from `sync-canonical.ts`), inserts its relations, transitions the candidate `pending→approved` under the same token, and appends the approval audit. Reusing the sync import path makes approve and rebuild produce byte-identical rows by construction (rebuild-equivalence, vertical-slice §F); on a step-8/9 failure after the file lands, a `canonical_db_divergence` dirty marker is recorded and a recoverable error returned (FR-053). reject/supersede/edit are DB-only optimistic transitions (no file). **proposal→canonical mapping**: a `KnowledgeProposal` lacks the canonical type-specific classification (`decision.kind`, `rule.severity`, `concept.domain`, …) and `scope.repository`; these are taken from an optional reviewer-set `classification` on the candidate draft (editable via `PATCH`), else a deterministic default (`decision.kind=implementation`, `rule.severity=warning`, `incident={medium,open}`, `pattern.maturity=emerging`, `insight/review_learning` category defaults, `concept.domain=""`), with `scope.repository` injected and Session/Checkpoint provenance prepended to `sources[]`; canonical `relations[]` keep only proposal relations whose type is a canonical relation type. This shapes canonical data, so it is recorded per the decision rule; it does not change the canonical v1 *format*. Session-Summary candidates are not approvable through the review queue (canonical-schema.md §8). **In-process write mutex** (`withRepositoryWriteLock`, promise-chain per repository id) serves step 1; a cross-process lock is still not provided — no primitive is scoped to any WP (ID-022/024(5)/026(10)/028(q)/030 lineage); the atomic file rename + next-sync repair bound the exposure. **Dashboard auth** (ID-015 / dashboard-api.md §3): a per-start 256-bit launch token is exchanged **once** (single-use → replay refused) for a random opaque HttpOnly + SameSite=Strict, process-lifetime cookie; every mutation additionally requires exact same-origin (`Origin` host === `Host`), JSON content-type, and an `X-Iroha-Request: 1` header a cross-site load cannot set; all §9 security headers + strict CSP are applied to every response (including static); auth state is in-memory and dies with the process. **Cross-package re-export consolidation**: the API needs `@iroha/domain`/`@iroha/config`/`@iroha/storage` types+values but may import only `@iroha/core`, so those are re-exported from the single hub `@iroha/core/mcp/facade.ts` (extended with `CandidateStatus`/`CandidateType`/`RepositoryConfig`/`repositoryConfigSchema`). A first attempt re-exported them from a second barrel (`dashboard/index.ts`); two `export *` sources exporting the same name made those names **ambiguous and silently dropped** from `@iroha/core`, breaking `@iroha/mcp`'s existing imports — fixed by keeping all cross-package re-exports in the one facade. **Typed client**: Hono RPC (`hc<AppType>`) was rejected — the DRY `respond`/`readJson` helpers take an untyped context and erase per-route response types, so RPC would yield `any`; instead `@iroha/api` re-exports each endpoint's response `data` type (from core) and the SPA uses a small hand-written typed `fetch` client (the "generated client" contract, compatibility.md §4, satisfied at the type level; the SPA depends on `@iroha/api` types only). **Static serving**: `createStaticHandler` serves the built SPA for non-API GETs with an index.html fallback (direct-route reload, §10); traversal is rejected on the raw path before any fs access, then the resolved real path is confirmed inside the resolved real root (symlink-safe, path-and-symlink-safety rule); the CLI passes `DASHBOARD_DIST` (resolved at `context.ts`'s src/dist-consistent depth) only when the dist exists. **Frontend stack** (ID-017): React Router **7.18.1** (dashboard-api.md §2's "8.2.0" does not exist on npm — latest real version used; the spec's other pinned frontend versions are treated the same way), TanStack Query 5.101.3, Tailwind **v4** 4.3.3 (CSS-first `@theme` tokens in `index.css`); biome needed `css.parser.tailwindDirectives: true` (to parse `@theme`), a `noDefaultExport: off` override for `*.config.*` (Vite/Vitest configs require a default export — the pre-existing exemption was latent until a config file was re-staged), and `public/` excluded from biome (it lints raw brand `.svg` assets under `noSvgWithoutTitle`). Component tests use vitest + jsdom + testing-library. **Deferred to a WP-09 follow-up (Stage 4/5)**: the Sessions/Work-Graph/Settings/Doctor pages, React Flow graph + Recharts charts, the Playwright E2E (kept opt-in/local, **not** added to the CI verify matrix — user-chosen, consistent with the project's opt-in-heavy-test posture), and the cross-artifact privacy scan; the API + all their backing `@iroha/core` use cases (`getSessionDetail`/`getRunDetail`/`graphQuery`/`graphPath`/`getSettings`/`updateSharedConfig`/`doctorRepair`/`getSyncStatus`/…) are already implemented and endpoint-wired, so the follow-up is UI-only. **Default UI language = English** (product-owner directive, 2026-07-21: "配布物は基本デフォルト英語"): the dashboard i18n fallback is `en` and `iroha init` already writes `config.default_language: en`; Japanese stays a selectable locale (full JA/EN message catalog) and a repo whose config says `ja` opens in Japanese. This **supersedes dashboard-api.md §8's "Japanese is the default UI locale" prose** (recorded in `.claude/rules/distributable-language.md`; the §8 wording will be corrected in a future doc pass). **Brand/design** (product-owner-provided): the organic "生成り" logo palette (paper `#F4EFE5`, ink `#2E2A22`, matcha `#6E7B57`, clay `#BC9870`, persimmon `#C26A3C`) applied as Tailwind `@theme` tokens with a warm-editorial system (tonal depth not shadow, three-dot motif, rounded display face, matcha as the single weight-bearing accent; CSP-safe system fonts only); brand assets committed under `apps/dashboard/public/`; recorded in `.claude/rules/brand-and-design.md`. **Latent gap noted, not fixed here**: `knowledge_items` is populated by no production code (neither sync/rebuild nor approve, which reuses the sync path), so `get_active_rules` / a Rule listing returns empty after a rebuild — a pre-existing WP-06/07/08 gap, left to the Rules/Guardrail-authoring WP alongside the already-deferred `severity` column and guard-spec schema, so approve stays rebuild-equivalent. None of the above changes the canonical v1 format, the MCP wire shape, or distribution compatibility; the auth/CSP surface implements dashboard-api.md §3/§9 as specified. | Accepted |
| ID-034 | WP-09 Stage 5 — the Playwright E2E deferred by ID-033 is now delivered as a new `apps/e2e` (`@iroha/e2e`) workspace package with one spec (dashboard-api.md §10). It spawns the **real built** `iroha dashboard --json --no-open` binary against a fresh temp Git repo it sets up per run (`git init` → `@iroha/core.runInit` + `resolveInitializedRepository` → seed one pending `decision` candidate straight into the local index via `@iroha/storage.insertCandidate`), then drives the shipped SPA through the whole human path: launch-token exchange → open the review queue → open the candidate → fill a reviewer name → approve → assert a canonical `.md` containing the decision title exists under `.iroha/` on disk (the core guarantee) → open it as approved knowledge → `page.reload()` to exercise the packaged server's SPA fallback. **Kept out of the CI verify matrix (user-chosen, ID-033):** `test:e2e` is a distinct Turbo task (`dependsOn: ["^build"]` so it builds the CLI + dashboard dist first); `ci.yml` runs only `lint`/`typecheck`/`test`/`build`, and `apps/e2e` defines **no `test` script**, so `pnpm test` never launches a browser. Browser binaries are never downloaded on CI — pnpm 11 blocks Playwright's install postinstall by default, and Chromium is fetched only by an explicit local `pnpm exec playwright install chromium`. The package **does** define `lint` + `typecheck` (both browser-free), so the harness is still gated by CI and cannot silently bit-rot. Catalog: the unused pre-staged `playwright` entry was replaced by `@playwright/test` 1.61.1. **Authoring note**: `startDashboardServer`'s reported `url` already carries the one-time `#token=…` fragment, so the spec navigates to it verbatim; an initial version appended its own `/#token=…`, duplicating the fragment so the SPA extracted a garbage token → `/auth/exchange` 401 → unauthenticated, which made `App`'s auth-gate early-return flap and continuously remount the header (Playwright saw the nav link "detached from the DOM" and timed out) — diagnosed by logging `console`/`response`/`framenavigated`, then fixed. No production code changed; this is test-only. | Accepted |
| ID-035 | WP-10 part 1 (`@iroha/vertical-slice`, `@iroha/core`) — the offline vertical-slice harness (`vertical-slice.md` steps A–F) plus the interruption/recovery flow (§5), and the two small production changes the slice requires. **Harness placement**: a new `apps/vertical-slice` (`@iroha/vertical-slice`) workspace package holds vitest integration tests, consistent with `apps/e2e` (ID-034) — placing it under `apps/*` (an existing workspace glob) keeps it out of the per-package `noRestrictedImports` dependency boundaries `biome.json` enforces on `packages/*`, which a cross-cutting consumer of `core`/`mcp`/`api`/adapters/storage would otherwise violate. It defines a `test` script (plain vitest), so `pnpm test`/CI's verify matrix runs it (acceptance: "Tier 1 CI"). The synthetic `repo-basic` fixture lives at repo-root `tests/fixtures/repo-basic/` (`vertical-slice.md` §2) — deliberately **outside** the `apps/*`/`packages/*` workspace globs so pnpm never treats its `package.json` as a workspace member; `biome.json`'s `files.includes` gains `!tests/fixtures/**` so the fixture (synthetic external-repo content) is not held to iroha's own lint rules. The harness builds a deterministic temp Git repo from the fixture, drives the real hook path (`runHook` with the exact raw Claude/Codex payload shapes the adapter tests use), the real agent-facing MCP path (`@iroha/mcp.dispatchTool` for `create_checkpoint`/`search`), and the human path (`@iroha/core.approveCandidate`, since there is no MCP approve tool), then simulates a teammate via `git clone` + rebuild. Codex parity is a separate `describe` (same normalized behavior: SessionStart token + approved rule, `apply_patch` → repo-relative target, Turn `checkpoint_state=pending`). **Production change 1 — `knowledge_items` population (closes ID-033)**: `importCanonicalDocument` (`sync-canonical.ts`) now projects every knowledge-type canonical document (all types except `session_summary`) into `knowledge_items` via `upsertKnowledgeItem` — a guardrail Rule carries its machine-evaluable `guard` object (canonical-schema §7) as `guard_spec_json`, everything else is `advisory`. This was a hard prerequisite for §B ("approved fixture Rule appears in context"): `listApprovedRulesForRepository` (SessionStart context, MCP `get_active_rules`) is `knowledge_items JOIN entities`, so an approved Rule was invisible until this projection existed. Because both `sync`/`--rebuild` and the WP-09 approval transaction funnel through this one function, approve and rebuild populate `knowledge_items` identically — **rebuild-equivalence (the invariant ID-033 preserved by leaving it empty) is preserved by populating it in the shared path**. `knowledge_items` still has no `severity` column (ID-030/032 — a Rule's severity lives only in canonical frontmatter/search), so `get_active_rules.severity` stays deferred; guardrail *evaluation* in PreToolUse remains WP-10 part 2. Regression test: `sync-canonical.test.ts` "projects approved rules and decisions into knowledge_items". **Production change 2 — unresolved-item injection at SessionStart**: `handleSessionStart` (`dispatch.ts`) now parses the latest checkpoint's `unresolved_json` and passes it as `recentCheckpoint.unresolved`. `context.ts`'s `formatSessionContext` already *rendered* `unresolved:` (hooks-contract §9) but nothing populated it, so §5's "last saved Checkpoint **and unresolved items** are injected" was a latent gap; the recovery harness is its reproduction. **Scope/interpretation choices, recorded per the decision rule (no canonical-format/DB-schema/public-API change — the `knowledge_items` table and columns already exist in migration 001):** (1) §F's FTS-only Japanese query — the spec's literal example `"なぜrepository patternを使うの？"` glues the Latin term to kana, and `buildMatchQuery` tokenizes on whitespace only, so neither the unicode nor trigram arm matches English content (consistent with ID-032: cross-lingual/mixed-script recall is a vector-arm concern, out of scope for the FTS-only path). The harness uses `"なぜ repository pattern を使うのか"` (Latin term whitespace-delimited) — a natural Japanese query that retrieves the approved Decision via the lexical arm, satisfying §F's intent without changing accepted search behavior. (2) Teammate-clone flow — a fresh `git clone` has the committed `.iroha/` but no local index, and `sync --rebuild` (`rebuildDatabase`/`replaceDatabaseAtomically`) fails trying to move aside a non-existent DB. The harness runs `iroha init` (which creates the DB and imports canonical) then `sync --rebuild` (which then has a DB to rebuild), proving rebuild-equivalence; that `sync --rebuild` on a never-initialized clone errors rather than creating fresh is left as a follow-up (a real but narrow onboarding-UX gap, not exercised by any acceptance criterion beyond this note). (3) A checkpoint proposal's `body` is used verbatim as the canonical document body, so it must already satisfy the canonical body-template (H1 == title + the type's required H2 sections); the slice's Decision proposal supplies a conforming body. Verified: `pnpm lint`/`typecheck`/`test`/`build` all green; the 11 harness tests (10 slice + 1 recovery) pass. | Accepted |
| ID-036 | WP-10 part 2 (`@iroha/core`) — PreToolUse **Guardrail evaluation**, the capability ID-024(6)/ID-028(l)/ID-030/ID-032 deferred "until a guard-spec schema exists" and ID-035 scoped here. The guard shape (`{tools, paths, deny_commands?}`) is defined in canonical-schema §7 and projected to `knowledge_items.guard_spec_json` (ID-035), so evaluation is now buildable. `handleToolStarted` (`hooks/dispatch.ts`) calls `evaluateActiveGuardrails` (`hooks/guardrail.ts`); a match records the Tool Event as `phase/status = denied` and returns `denyOutput(ruleId, reason)` → the adapters render hooks-contract §6.3's exact `permissionDecision: "deny"` / `"Blocked by iroha rule <id>: <reason>"` (reason = entity `summary ?? title`). **Matching is operation-based and tool-agnostic (product-owner decision, 2026-07-21).** A path Guardrail denies the first `file`/`path` target with a **write/delete** operation under a protected `guard.paths` glob, *regardless of the tool name*. This was chosen over exact `guard.tools` string matching after a fresh-context adversarial review reproduced a **HIGH bypass**: a guard listing `["Edit","Write"]` was silently evaded by `MultiEdit`, `NotebookEdit`, and Codex `apply_patch` — all real file-write tools the adapters classify to `operation:"write"`/`"delete"` (Claude `parse-event.ts`, Codex `apply_patch` header extraction). Keying on the adapter-normalized *operation* is the concrete implementation of §6.3 step 1 "normalize tool name" and is fail-safe/forward-compatible (a future write tool is covered automatically); `guard.tools` is retained (canonical `min 1`) as approved author intent and doctor/health metadata, but is not the enforcement gate. Reads (`operation:"read"`) of a protected path are allowed. The denied Tool Event records the *matched* target (a patch can touch several files — the audit names the one that triggered the block), and persists only `target_summary`/`input_digest` — no patch/edit body (verified by a harness marker assertion). **Fail-open** is layered: `evaluateActiveGuardrails` → null on a query error; `parseGuardSpec` → null on malformed JSON or empty `tools`; the outer `runHook` catch swallows throws — an internal failure never blocks the agent (§7: CI is the hard enforcement layer). **`iroha doctor` gains a `guardrails` check** classifying each active guardrail spec as `enforceable` (parses + has paths), `not_hook_enforceable` (parses but no paths — a command/`deny_commands`-scoped guard), or `invalid` (malformed/empty `tools`), warning on the latter two so a silent no-op Guardrail never reads as healthy (the adversarial review's second HIGH: a no-path guard reported "all evaluable"). **CI enforcement**: `guardrailPathViolations(rules, changedPaths)` is a pure, tool-independent path check (a changed path under a protected glob is a violation) sharing the spec-parse + `pathMatches` with the Hook — the primitive a CI job calls; the actual `.github/workflows` wiring is WP-10 part 3 (10c). **Deferred / accepted, recorded per the decision rule:** (a) `deny_commands` is enforced by no layer — the Hook cannot (the raw command is not available post-classification, §8; only the `classifyCommandTarget` leading token is), and the CI path check operates on file diffs, not commands — so a command Guardrail is surfaced by doctor as `not_hook_enforceable` rather than silently trusted; enforcing `deny_commands` needs a raw-command CI check, a future WP. (b) `pathMatches` is reused unchanged — its "simplified prefix glob" means a single `*` (e.g. `src/config/*`) over-blocks nested paths in a deny; this is the fail-safe direction (over-block, never under-block) and is accepted rather than forking the matcher shared with `get_active_rules`. (c) A header-less/opaque Codex `apply_patch` degrades to a `kind:"other"` target, which the Hook cannot path-match — the CI diff check is the backstop (it sees the actual changed files). (d) The `guard_spec_json` `json_valid` CHECK (migration 001) means a stored spec is always valid JSON, so "unevaluable" means *structurally* invalid (empty `tools`), not bad JSON — the corruption test uses `{"tools":[]}`. **Pre-existing cross-contract inconsistency surfaced (not introduced here):** `schemas/canonical-v1.schema.json` uses `deny_commands` (snake_case) while `schemas/checkpoint-v1.schema.json` uses `denyCommands` (camelCase); moot for the Hook path (unenforced either way), to be reconciled in a future doc/schema pass. `knowledge_items.severity` and cross-process locking remain untouched/deferred. Verified: `pnpm lint`/`typecheck`/`test`/`build` all green; 7→ the guardrail unit tests + doctor tests pass; the harness `guardrail.test.ts` (deny incl. tool-agnostic MultiEdit / unrelated-allow / CI path check / fail-open + doctor warning) passes. Reviewed by fresh-context spec-compliance (substantively compliant) and adversarial (HIGH bypass + HIGH false-"ok" → both fixed as above; residual LOWs accepted/deferred as noted). | Accepted |
| ID-037 | WP-10 part 3 (`@iroha/vertical-slice`) — the 10,000-entity **performance regression fixture** (`performance.test.ts`) and the **cross-artifact privacy scan** (`privacy.test.ts`), completing WP-10's acceptance. Test-only; no production code changed. Both live in `apps/vertical-slice`, so the existing `test` Turbo task runs them across the CI verify matrix (acceptance: "Tier 1 CI") — no new workflow needed. **Performance (§7)**: 10k `entities`+`search_documents` rows are bulk-seeded via `db.batch` (chunks of 500; FTS auto-populates through the `search_documents_ai` trigger). Per §7's "regression gates after a stable baseline is recorded", the fixture `console.log`s measured p50/p95/max and asserts **generous ceilings** (~10–30× the p95 targets: search 3s, SessionStart 5s, guardrail 2s) so a gross regression (an N+1, a full scan, a pathological glob) fails while ordinary shared-runner variance does not (the product owner chose to keep these in the default CI matrix, so tight p95 gates would flake). Only the **lexical-search** measurement truly exercises 10k scale (the query "synthetic benchmark" OR-routes and every seeded doc matches, so FTS traverses all 10k); SessionStart and guardrail evaluation query `knowledge_items`/checkpoints, not `search_documents`, so their latency is largely independent of `ENTITY_COUNT` — honest weak gates, recorded as such (a full-`entities`-scan regression would still be caught since 10k `entities` rows exist). **rebuild@10k is not benchmarked**: `sync --rebuild` imports from canonical files, so a 10k-scale rebuild benchmark would need 10k `.iroha/**/*.md` files (heavy I/O); rebuild is exercised functionally in the slice (§F) and rebuild-*equivalence* is asserted there, so a 10k rebuild timing gate is deferred. Dashboard-API latency (§7's ≤500ms) is not measured (would need the running server); its backing `@iroha/core` use cases run in the slice. **Privacy (§6)**: the loop is driven with seeded raw markers (a prompt marker, an edit-body marker, and a detectable RSA-private-key secret in a checkpoint free-text field), then scans (1) every file under `.iroha/**`, (2) **every string cell of every real table** (schema-driven via `sqlite_master`, so no column is missed — the meaningful guard: a raw prompt/patch stored in `turns.intent_summary`/`tool_events.target_summary`/a checkpoint JSON column/`search_documents.body` would be caught), and (3) MCP responses. Asserts the raw prompt, raw patch, the checkpoint **secret** (must be redacted by WP-07 `redact.ts`), the `ist_` token plaintext (stored only as `hmac-sha256:`), and the absolute fixture path never appear. **Fresh-context adversarial review found and fixed two false-greens**: (i) HIGH — the guardrail perf measurement ran *after* the SessionStart it-block's `resume`s left a turn-less Run, so PreToolUse early-returned (`turn === null`) before ever reaching `evaluateGuardrails`, timing a no-op; fixed by establishing a fresh startup-session + prompt and asserting `tool_events.status='denied' > 0` to prove the guardrail path was actually reached. (ii) MEDIUM — the MCP-response privacy assertion was vacuous (the prompt/patch markers never reach an MCP-returnable surface); fixed by seeding the secret into the checkpoint summary and asserting `get_session_state` (which echoes the recent checkpoint summary) does not leak it — exercising the redaction path end-to-end. Accepted residuals (adversarial LOWs): the `.iroha/**` marker/token/abs-path assertions are defense-in-depth (those seeds don't naturally flow toward canonical; the DB-cell scan is the live guard); "structured logs" are not scanned because `event_log` is written by no production code (ID-028(r)) — there is no log artifact; the built **plugin archive** privacy scan is deferred to WP-11 (the archive does not exist until packaging). Verified: `pnpm lint`/`typecheck`/`test`/`build` all green; the full harness is 20 tests (slice 10 / recovery 1 / guardrail 3 / performance 3 / privacy 3). This completes WP-10 (all Vertical Slice assertions, cross-artifact privacy scan, Tier 1 CI, rebuild-equivalence). | Accepted |

| ID-038 | WP-11 part 1 (`@iroha/plugin`, plus small re-export refactors in `@iroha/core`/`@iroha/mcp`/`@iroha/cli`) — the packaged plugin surface: shared metadata, both platform manifests, hook/MCP configs, the six shared Skills, the bundled `iroha` binary, thin-archive assembly, and the package smoke test. Manifest/hook/skill/marketplace formats were verified against the current official Claude Code (code.claude.com) and Codex (learn.chatgpt.com) docs before implementing, not assumed. **Native-dependency archive strategy — product-owner decision (Option A), the pivotal distribution choice.** `@iroha/storage` uses `@libsql/client`, which loads a per-platform native `.node` binding (`@libsql/darwin-arm64/index.node` etc.), so the hook/MCP/CLI entrypoints cannot be inlined into a single self-contained `.mjs` — the spec's §10 "node + args exec form" and §13 "archive runs without node_modules / no install script" cannot both hold for a native-dependent bundle. Presented with (A) npm owns the runtime + a thin plugin archive, (B) per-platform self-contained archives, (C) one fat archive carrying all platform natives, the product owner chose **(A)**: the published npm package `@iroha-labs/iroha` carries the `iroha` binary and its native `@libsql/client` (resolved per platform by npm's `optionalDependencies`), and the plugin archive ships only text — the two manifests, hook/MCP config, and skills. Consequence recorded per the decision rule (distribution compatibility): the manifest hook/MCP commands invoke the PATH binary **`iroha`** (`iroha __hook <platform>`, `iroha __mcp`) rather than `node ${CLAUDE_PLUGIN_ROOT}/dist/*.mjs`; this is a deliberate deviation from §10's exec-form wording, consistent with §10's own "CLI fallback is always documented" and ID-011 ("the executable remains `iroha`"). The affected contract prose that had described the old `node <plugin-root>/dist/hook.mjs`/`dist/mcp.mjs` entrypoints — hooks-contract §2/§3, mcp-contract §2, and compatibility §10 — was updated in this same change to describe the `iroha __hook <plat>`/`iroha __mcp` invocations, so no prose-vs-implementation conflict is left standing (DoD "affected documentation is updated"). **Binary/architecture**: the published `@iroha-labs/iroha` is `@iroha/plugin` (the only package `compatibility.md` §4 lets depend on `cli`+`mcp`+adapters — the superset needed to route user commands, the hook, and the MCP server from one entry). `bin.ts` is a thin, import-light loader that statically imports only `metadata.ts` and pulls the heavy graph in via a dynamic `import("./dispatch.js")`; `dispatch.ts` routes `__mcp`→`@iroha/mcp.startStdioServer`, `__hook <plat>`→`runHookMain`, else→`@iroha/cli.runCli`. The split is load-bearing for the hook's fail-open invariant (fresh-context adversarial review, reproduced): a transitive dep (`rc-config-loader` via `secretlint`) calls `process.cwd()` at module top level, which throws `ENOENT` when the agent's working directory was removed mid-session — an *uncatchable* ESM module-load crash if the graph were statically imported by the entry, but a catchable `import()` rejection here, so `bin.ts` still exits 0 for `__hook` (and each dispatch branch additionally `.catch`es runtime failures: `__hook`→exit 0, `__mcp`→one stderr line + exit 1). `@iroha/cli`'s own `bin.iroha` was removed so `@iroha/plugin` is the sole `iroha` binary owner (its bin is the superset with `__hook`/`__mcp`); `link:global` now links `@iroha/plugin`. Because §4 forbids `@iroha/plugin`→`@iroha/core`, the hook runner is reached via `@iroha/cli` re-exporting `runHookMain` (cli→core is allowed); `@iroha/core`'s prior unused `main()` (read `process.argv[2]`) was replaced by `runHookMain(platform)`, and `@iroha/mcp`'s stdio wiring was extracted from `main.ts` into an exported `startStdioServer()` (DRY — `main.ts` and the plugin bin now share it). tsdown `deps.alwaysBundle: [/^@iroha\//]` bundles the private workspace packages into `dist/bin.mjs` (402 kB) while every npm dep — including the native `@libsql/client` — stays external and is declared in `@iroha/plugin`'s `dependencies` (the union of non-`@iroha` runtime deps across the bundled graph), so `npm install` resolves them. **Manifests generated from one `metadata.ts`** (§10): Claude `.claude-plugin/plugin.json` (`hooks`→`./hooks/claude.json`, `mcpServers`→`./.mcp.json`) and Codex `.codex-plugin/plugin.json` (`hooks`→`./hooks/codex.json`, `mcpServers`→`./mcp.codex.json`). The two platforms' formats genuinely differ, so they are separate files: Claude hooks use exec-form `command`+`args` (`"iroha"`,`["__hook","claude"]`); Codex hooks use a single shell string (`"iroha __hook codex"`) and only `type:"command"` handlers; Claude MCP config keys `mcpServers`, Codex keys `mcp_servers` (snake) — distinct filenames avoid a same-name/different-shape collision. All seven P0 hook events (SessionStart, UserPromptSubmit, Pre/PostToolUse, Pre/PostCompact, Stop — Codex lacks SessionEnd, which the P0 set does not use) dispatch the same `iroha __hook <plat>` command; the hook discriminates on the stdin `hook_event_name`. Per-event manifest `timeout`s are whole-second ceilings ≥ the hooks-contract §7 budgets (SessionStart 3, UserPromptSubmit 2, Pre/PostToolUse 1, Pre/PostCompact 1, Stop 2) — the internal p95 targets are far smaller and the hook is independently fail-open (Pre/PostToolUse round §7's 0.5s/0.75s up to the integer 1, matching §3's own `"timeout": 1` example). **"Platform manifest validators pass" (acceptance)** is implemented as offline Zod schemas (`manifests.ts`) that the package smoke test runs against every generated file; the `claude`/`codex` CLIs may additionally validate when installed but are not required in CI (no agent runs in CI, consistent with ID-028(o)). **Skills** are `skills/<name>/SKILL.md` with `name`+`description` frontmatter only — the one shared directory is valid on both platforms (Codex documents no richer frontmatter); bodies are CLI-oriented because that is the reliable cross-platform path. **Smoke test (`test:package`)**: assembles the archive into a temp dir and asserts both manifests + hook + MCP configs validate, all six skills carry frontmatter, the archive is thin (no `dist`/`node_modules`/home-path leak), the built binary's MCP `tools/list` contains the eight agent tools and **no** approve/reject/publish/delete/activate tool (mcp-contract §4), and `iroha __hook claude` is fail-open (empty output, exit 0) both outside an initialized repo and when its working directory is removed mid-run (the module-load `process.cwd()` regression above). Split from `test` (which excludes it) because it spawns the package's own `dist/bin.mjs` and so needs `dependsOn: build`. **Assumptions from the Codex doc research, recorded not silently resolved** (Codex surfaces are Preview per §8; all reversible, none touching canonical data/DB schema/MCP wire shape): (a) `$iroha:<skill>` namespaced invocation is **not** documented for Codex (only flat `$name`) — the CLI (`iroha <cmd>`) is documented as the reliable path, matching §10's hedge; (b) no manifest field carries a minimum Codex version — the 0.144.5 floor lives in prose docs + `iroha doctor`'s runtime check (WP-11b); (c) Codex hook matcher-optionality and shell-vs-exec of the `command` string are unstated — mitigated by omitting matchers (fire on all) and by `iroha __hook codex` needing no shell features; (d) Codex `.mcp.json` env-var expansion / relative-path resolution is unverified — sidestepped entirely by invoking the PATH binary `iroha` (no path or env var in the command). **Deferred to WP-11b** (release/distribution, human-gated area): the two repository marketplaces, the release workflow (SHA-256 checksums, SBOM, npm provenance, GitHub artifact attestation, clean-checkout build) and its version-consistency CI gate (a `PLUGIN_VERSION === package.json` unit test is in place now as the seed), install/update/uninstall docs, `iroha doctor`'s manifest-validation + Codex hook-trust enrichment (bundled with the install-onboarding docs, and because `@iroha/core`'s doctor cannot import `@iroha/plugin`'s schemas without inverting §4), the optional `repository`/`homepage`/`license` manifest fields (set at release with confirmed URLs and the ID-019 license choice), and the built-archive privacy scan carried over from WP-10 (ID-037). Verified: `pnpm lint`/`typecheck`/`test`/`build` and `pnpm test:package` all green (core 121 pass/1 skip, mcp 11, cli, plugin unit 9 + smoke 8 unchanged elsewhere). | Accepted |

| ID-039 | WP-11 part 2 (`@iroha/plugin`, repo-root `LICENSE`/`.claude-plugin/`/`.agents/`, `docs/install.md`) — license, repository metadata, the two repository marketplaces, and the install/update/uninstall docs. **License — product-owner decision (resolves ID-019, the release-blocking human gate): Apache-2.0.** The canonical text is committed verbatim as repo-root `LICENSE` (fetched from apache.org), `license: "Apache-2.0"` is set on the root and `@iroha/plugin` `package.json` and on both generated platform manifests, and `repository`/`homepage` on the manifests + package.json now point at the **real** remote `https://github.com/iroha924/iroha` — not the `iroha-labs/iroha` a research prompt had assumed; WP-11a deliberately omitted these optional URL fields precisely so no wrong value was baked in before the remote was confirmed (`git remote` shows `git@github.com:iroha924/iroha.git`; the npm scope `@iroha-labs/iroha` and the GitHub org `iroha924` differ, which is fine — ID-011 fixes only the package name and binary). `metadata.ts` gained `PLUGIN_LICENSE`/`PLUGIN_REPOSITORY`/`PLUGIN_HOMEPAGE`/`PUBLISHED_PACKAGE_NAME`, and the manifest Zod schemas now require `homepage`/`repository` (`z.url()`) and `license`. **Marketplaces (compatibility.md §13.3/§13.4), both generated from `metadata.ts` and source = npm.** Choosing `{ source: "npm", package: "@iroha-labs/iroha" }` for both is the coherent Option A choice (ID-038): npm carries the binary, its native `@libsql/client`, and — once WP-11c wires publishing — the plugin config, so a single `npm` source installs everything the manifests reference; no other source (github-clone of un-built config, or a per-release archive) has all three. Both the Claude Code plugins-reference and the Codex build-plugins marketplace schemas document `npm` as a valid plugin `source` type (`{ source: "npm", package, version?, registry? }`) — confirmed in the WP-11a doc research — so the committed files are structurally valid; end-to-end acceptance by the live loaders is confirmed at the WP-11c publish step, since they are not installable until the renamed package is published. The plugin entry pins **no** `version`, so the npm package's own version applies and the committed marketplaces never need a version bump. They are checked-in generated files at the platform-expected locations — Claude `.claude-plugin/marketplace.json`, Codex `.agents/plugins/marketplace.json` (the Codex-docs-preferred path over the legacy `.claude-plugin/` one) — written by `build-archive-cli.ts` (`writeMarketplaces(REPO_ROOT)`, `REPO_ROOT` resolved via `../../..` from the src/dist-consistent depth) alongside the archive; `marketplaces.test.ts` (a `test`-task unit test, no build needed) asserts each committed file deep-equals its generator output, so a `metadata.ts` change that is not regenerated fails CI (implementation-plan §4 "generated files are reproducible"). Committing `.claude-plugin/marketplace.json` at the repo root is inert for this repo's own dev sessions — the file is only consulted after an explicit `/plugin marketplace add`, and no `plugin.json` is committed at root, so the repo is not itself a plugin. **Docs (`docs/install.md`, English per distributable-language)**: leads with the required `npm i -g @iroha-labs/iroha` (the reliable cross-platform path — the `iroha` CLI works with no plugin), then the Claude (`/plugin marketplace add iroha924/iroha` → `/plugin install iroha@iroha` → `/iroha:<skill>`) and Codex (`codex plugin marketplace add` → `/plugins` install → **`/hooks` to trust**, since install ≠ trust) flows, honestly flagging the Codex-doc uncertainties surfaced in the WP-11a research (no documented `$iroha:<skill>` namespace — flat `$init`; no single documented `codex plugin install` command) and routing around them via the CLI fallback. **Deferred to WP-11c** (the release-automation PR): the release workflow (SHA-256 checksums, SBOM, npm provenance, GitHub artifact attestation, clean-checkout build), the publishing config that renames/repackages `@iroha/plugin`→`@iroha-labs/iroha` and includes the generated plugin config in the npm tarball (which is what makes the `npm`-sourced marketplaces functional — until then the marketplaces are structurally valid but not yet installable), the version-consistency CI gate over package/manifests/changelog/tag, `iroha doctor`'s manifest + Codex hook-trust checks, and the built-archive privacy scan carried from WP-10 (ID-037). Verified: `pnpm lint`/`typecheck`/`test` (plugin unit 14 = 9 manifest/version + 5 marketplace) and `pnpm test:package` (9) all green. | Accepted |

| ID-040 | WP-11 part 3 (`@iroha/plugin`, `.github/workflows/release.yml`) — the release candidate: the publishable-package assembly, the release workflow, and version consistency. **Publish packaging** (`build-release.ts` → `assembleRelease(destDir)`): reuses `assembleArchive` for the plugin config + skills, copies the built `dist/` and `LICENSE`, and writes a generated `package.json`. Per ID-011 (internal `@iroha/*`, published `@iroha-labs/iroha`) the generated manifest renames the package, **drops the `@iroha/*` workspace deps** (bundled into `dist/bin.mjs` by tsdown, not runtime deps — dropping them is essential or the published package would declare non-existent `@iroha/cli@0.1.0` etc.), and **resolves every `catalog:` dep to its concrete version** from `pnpm-workspace.yaml` (parsed with `yaml`), so the tarball installs with no workspace or catalog. `dist/` is **pruned to the `bin.mjs` import closure** (`pruneDistToRuntime`, a hash-name-robust graph walk over `./*.mjs` static+dynamic imports — keeps `bin`+`dispatch`+`metadata`, drops the `index` lib entry, the `build-*-cli` tooling and its chunks, and all `.d.mts`), so the runtime package ships no build tooling. `files` lists `dist`/`skills`/`.claude-plugin`/`.codex-plugin`/`hooks`/`.mcp.json`/`mcp.codex.json` (npm includes dot-dirs when explicitly listed — confirmed via `npm pack --dry-run`: 17 files, 116 kB). **`release-pack.test.ts`** (`test:package`) drives `npm pack --dry-run --json` and asserts the tarball name/version, that the binary + both manifests + hook/MCP config + skills + LICENSE are present, that source/tests/tooling/`.d.mts`/`node_modules` are absent, that no dependency is a `@iroha/*` or unresolved `catalog:`/`workspace:` spec, and (compatibility.md §5) that **no shipped file contains the build machine's absolute repo path** — this **addresses the built-archive privacy scan ID-037 deferred to WP-11**. The §5 check keys on the real `REPO_ROOT`, not any `/Users/…`: the bundle legitimately contains authored placeholder examples (`/Users/alice`) in `@iroha/git`/`@iroha/platform` redaction docstrings, which are not a developer-home leak; verified the real build path and any `.map` file are absent. **Release workflow** (`release.yml`): **`workflow_dispatch`-only** (plan §7 makes publishing a human-authorized action — it never runs on push and never auto-publishes), default **dry run** (builds the package + tarball + SHA-256 checksums + CycloneDX SBOM via `anchore/sbom-action` + `actions/attest-build-provenance` build attestation, uploads artifacts, but does not publish); `npm publish --provenance --access public` and `gh release create` run only when dispatched with `publish: true` (npm auth via a human-provided `NPM_TOKEN` secret). A version-consistency step fails the run if the dispatched `version` ≠ the built package version, and `manifests.test.ts` already gates `PLUGIN_VERSION` ≡ `package.json`. **ci-discipline**: the verifiable steps (`pnpm build`, `build:release`, `npm pack`, `sha256sum`, the version check) were run locally and produce `iroha-labs-iroha-0.1.0.tgz` as expected; the SBOM/attest/publish steps depend on external actions and a real registry and **cannot be exercised in CI** — they run for the first time at a real, human-authorized release, and are documented as such in the workflow header (a `workflow_dispatch`-only file is dormant, so it does not affect PR CI). **Fresh-context adversarial review found two HIGH defects — the published package was broken — both fixed here.** The bundled CLI resolved `migrations/` and the dashboard SPA at `../../../` (repo-root-relative — correct only inside the monorepo, exactly what `context.ts` flagged for WP-11 to fix), so an installed `@iroha-labs/iroha` resolved them to `node_modules/migrations` / `node_modules/apps/…` (nonexistent): `iroha init` / `sync --rebuild` died with ENOENT and `iroha dashboard` silently served no UI — and CI stayed green because it runs from monorepo source (works-in-dev / broken-in-dist). Fixed by (a) `context.ts` resolving each asset package-relative first (`../migrations`, `../dashboard`) then repo-root; (b) `build-release.ts` shipping `migrations/` and the dashboard SPA in the tarball (added to `files`; 32 files, 481 kB); (c) `release-pack.test.ts` now unpacking the assembled package into an installed layout (its own `node_modules` symlinked to the plugin's, which declares exactly the runtime deps incl. native `@libsql/client`) and actually running `iroha init`, asserting `.iroha/config.yaml` — the functional class the path-only assertions had missed. Also hardened per the review's lower-severity notes: the workflow's `version` input is passed via `env` (no shell interpolation), `pruneDistToRuntime` skips a non-existent chunk name rather than throwing on a stray `"./x.mjs"` string, and `buildPublishManifest` throws on any unresolvable dependency spec (`workspace:` / named `catalog:` / `npm:` / `link:` / `file:`) instead of emitting an uninstallable one. **Deferred (the one remaining WP-11 §9 item), recorded with its architectural reason**: `iroha doctor`'s plugin-manifest validation is not added — `@iroha/core` (where `doctor` lives) may not import `@iroha/plugin`'s Zod schemas without inverting compatibility.md §4, and cannot reliably locate the installed manifests from the CLI process; the Codex hook-trust guidance already lives in the `doctor` skill, and "platform manifest validators pass" is satisfied at build time by the smoke tests. A future follow-up may add a lightweight structural (parse + required-field) doctor check gated on `CLAUDE_PLUGIN_ROOT`. **Human gates remaining**: the actual npm publish + GitHub Release (dispatch `publish: true` with `NPM_TOKEN`), and the marketplace listings go live once that publish exists. Verified: `pnpm lint`/`typecheck`/`test`/`build` and `pnpm test:package` (plugin: unit 14, smoke 15 across archive-smoke + release-pack) all green. | Accepted |

| ID-041 | WP-11 follow-up (`@iroha/core` doctor) — the `iroha doctor` plugin-manifest validation ID-040 deferred, the last compatibility.md §9 doctor item. `checkPluginManifests(root)` (in `doctor.ts`) replaces the `plugin-manifests` "not yet implemented (WP-11)" placeholder. It reads `<root>/.claude-plugin/plugin.json` and `<root>/.codex-plugin/plugin.json` and does a **structural** check — JSON parse + required non-empty `name`/`version` — **not** the plugin's Zod schema, because `@iroha/core` may not depend on `@iroha/plugin` (compatibility.md §4); the schema validators run at build time (`@iroha/plugin`'s package smoke test), and reading a file at a platform-convention path is not a code dependency. The root is `CLAUDE_PLUGIN_ROOT` / `PLUGIN_ROOT` (Codex sets both) when set, else the package root one level above this bundled module — under Option A (ID-038) the manifests ship in the same npm package as the binary (`<pkg>/dist/*.mjs` → `<pkg>/…`), so a plain `iroha doctor` from an installed package finds them without any env var. Reports `ok` "not running from an installed iroha plugin" when neither manifest is present (a terminal `iroha doctor` outside a plugin install — not an error), `ok` "valid: Claude iroha@0.1.0, Codex iroha@0.1.0" when present and well-formed, and `error` only when a present manifest is malformed JSON or missing a required field. Tests: four `doctor.test.ts` unit cases (well-formed / absent / invalid-JSON / missing-field, via a fixture root — `checkPluginManifests` is exported for this) plus the `release-pack.test.ts` installed-layout E2E, which now also runs `iroha doctor --json` against the assembled package and asserts the `plugin-manifests` check is `ok` with the shipped `iroha@0.1.0` manifests — proving the module-relative resolution finds them where `build-release.ts` ships them. The Codex hook-trust guidance (compatibility.md §9 step 5) remains in the `doctor` skill's prose, since `iroha doctor` cannot reliably read Codex's per-hash trust store from outside Codex. This completes WP-11's doctor deliverable; the only WP-11 gate left is the human-authorized publish (plan §7). Verified: `pnpm lint`/`typecheck`/`test` (core 125 pass/1 skip) /`build` and `pnpm test:package` (16) all green. | Accepted |
| ID-042 | WP-12 part 1 (`@iroha/forge` port + `@iroha/forge-github` GitHub provider) — the offline, unwired provider layer (no core/sync wiring yet; that is PR2, and review-learning candidates + surface is PR3). **`@iroha/forge`** (dep: domain only, §4) defines the `ForgeProvider` interface (`listIssues`/`listPullRequests` taking `{owner,repo}` + a `since` watermark, returning `Result<{items, watermark}, IrohaError>`), the provider-agnostic `Normalized{Actor,Issue,PullRequest,ReviewThread,ReviewComment}` types the Work Graph consumes, and the `forgeUnavailable(msg,{retryable})` degrade helper. It **re-exports** `ok`/`err`/`isOk`/`isErr`/`IrohaError`/`Result` from `@iroha/domain` so `@iroha/forge-github` — which §4/biome restrict to depend on `@iroha/forge` only — builds `Result` values without importing domain (same re-export pattern as plugin↔cli↔core; no §4/biome change). **GitHub provider — product-owner choice: octokit + GraphQL, "rich" (not raw fetch).** Runtime stack `@octokit/core` composed with `@octokit/plugin-retry` + `@octokit/plugin-throttling` + `@octokit/plugin-paginate-graphql`; the token is passed only to Octokit's `auth` (never logged/serialized). GraphQL was chosen over REST because it fetches issues/PRs/review-threads/comments in one nested query (fewer round-trips, better rate-limit economy), gives native cursor pagination that maps onto `sync_cursors`, and exposes `reviewThreads.isResolved`/`isOutdated` (→ `review_comments.resolution_state`) and `pullRequest.closingIssuesReferences` (→ PR `ADDRESSES` Issue) that REST does not surface cleanly. **Typed via graphql-codegen** against the SDL `@octokit/graphql-schema` ships (offline — no network; `codegen.ts` resolves the SDL through the package's per-package symlink anchored to the config dir, because the package's `exports` map exposes only the `import` condition so neither `./package.json` nor the main is `require.resolve`-able). Codegen uses `typescript-operations` + `typed-document-node` with `documentMode:"string"` and `preResolveTypes:true` and **no `typescript` base plugin** — this keeps the committed `src/generated/graphql.ts` at ~170 lines (inlining the two operations' scalar/enum types) instead of dumping GitHub's ~600 schema enums, and emits each operation as a `TypedDocumentString` (a `String` subclass) so the runtime needs no `graphql`/`print()` — the query string is passed straight to `octokit.graphql.paginate.iterator<T>(doc.toString(), vars)`. Consequence recorded per the decision rule (distribution compatibility): the octokit runtime plugins + `@graphql-typed-document-node/core` become runtime deps of the published `@iroha-labs/iroha` (the bundled `@iroha/*` graph's external deps), an accepted cost of the "rich" choice; `graphql` and the `@graphql-codegen/*` packages stay devDependencies (codegen-time only, thanks to `documentMode:"string"`). **Reproducibility** (implementation-plan §4): the generated file is committed and guarded by `codegen.drift.test.ts`, which asserts each `.graphql` operation body (with GraphQL's insignificant whitespace + commas stripped) appears inside the committed `TypedDocumentString` — a fast check that forces a regen (`pnpm --filter @iroha/forge-github codegen`) if a query is edited but not regenerated, without re-running codegen in the unit suite. **Security/robustness**: every GraphQL response is validated at the boundary with Zod (`normalize.ts`, lenient `z.object` that strips unknown keys and treats enum-like fields as free strings so a future GitHub enum value degrades to `"unknown"` rather than rejecting the page — forward-compatible per the unknown-field invariant); on a bad shape the provider returns a non-retryable `FORGE_UNAVAILABLE`. Thrown transport/octokit errors are **never serialized** into `IrohaError` — `mapForgeError` reads only the numeric HTTP `status` (403/429/5xx → retryable, 401/404/4xx → non-retryable) and builds a generic message, so the token cannot leak; a unit test asserts the token is absent from the fully-stringified error even when the response body echoes it. **Incremental**: queries order by `UPDATED_AT DESC` and the provider stops early when a node's `updatedAt <= since`, returning the fully-paginated new set plus the newest accepted `updatedAt` as the watermark (null when nothing new, so the caller keeps the prior cursor); a `maxPages` bound caps a runaway backfill. A `resilience` option (default true) lets tests disable retry/throttling so error-path assertions return without backoff. Review-comment bodies are stored only as a bounded 500-char summary. `catalogMode: strict` required adding the octokit + codegen versions to `pnpm-workspace.yaml`'s catalog; `biome.json` now ignores `**/generated`. Not wired anywhere yet (nothing imports `@iroha/forge-github`; `@iroha/core` still only declares `@iroha/forge`). A fresh-context `security-diff-reviewer` pass confirmed the core security properties hold (no token leak — the token reaches only Octokit's `auth`; every failure degrades to a `Result`; the GraphQL boundary is Zod-guarded) and surfaced four incremental-sync correctness bugs, all fixed here: (1) a missing/forbidden repo throws a GraphQL `errors` envelope before the `repository === null` branch can run, so `mapForgeError` now reads the discrete GraphQL error-type enum (`NOT_FOUND`/`FORBIDDEN` → non-retryable, lone `RATE_LIMITED` → retryable) instead of misclassifying it as a transient retry-forever; (2) hitting the page bound now sets a `truncated` flag on the result (`DEFAULT_MAX_PAGES` raised to 200) so a large delta's dropped tail is signalled rather than silent; (3) the early-stop is strict (`<`), so an item sharing the watermark's exact second is re-fetched (idempotent upsert dedupes) rather than skipped forever; (4) timestamps are compared as epoch millis (`Date.parse`) so ordering survives a future fractional-seconds format. The two list methods now share one `collectIncremental` helper so the fixes cannot diverge. Verified: full `pnpm lint`/`typecheck`/`test`/`build` green; `@iroha/forge-github` 14 tests (11 provider contract/security + 3 codegen drift), `@iroha/forge` builds clean. | Accepted |
| ID-043 | WP-12 part 2 (`@iroha/core` forge sync wiring, `@iroha/config` `forge.api_token_env`, `@iroha/storage` `getReviewCommentByExternalId`) — wires the PR1 provider into `iroha sync`. **Config**: `forgeConfigSchema` gains `api_token_env` (`envVarNameSchema.default("GITHUB_TOKEN")`, so configs written before this field parse unchanged); the serializer and `buildDefaultConfig` write it. The token *value* is read from `process.env` at sync time and never stored in config/DB/logs — the same posture as embedding's `api_key_env`. **`resolveForgeProvider(config.forge, env)`** mirrors `resolveEmbeddingProvider`: returns a `createGitHubProvider` when forge is enabled, the provider is `github`, and the token env var is non-empty; else `null`. **`resolveGitHubRef(cwd)`** parses `owner/repo` from the already-credential-stripped `origin` remote (`getSanitizedRemoteUrl`), matching https/ssh/scp `github.com` forms and returning `null` for non-GitHub/local remotes. **`runForgeSync` is fully non-fatal by design** (the "Forge failure must not fail canonical/Git sync" invariant, design.md §12): it returns a `ForgeSyncOutcome` (`disabled`/`skipped`/`synced`/`error`), **never throws and never returns `err`** — provider outages, rate limits, and DB write errors are captured in the outcome and recorded on the `github` sync cursor, so the canonical/embedding sync the run already completed is preserved. Per-resource watermarks live in `sync_cursors.state_json` as `{issues, pullRequests}` (two independent `updatedAt` streams need two watermarks under one provider cursor); a provider failure records `lastAttemptAt`/`lastErrorCode` without advancing them (the COALESCE upsert keeps the last-known-good cursor). `runSync` calls it after canonical + embedding via a non-fatal `syncForge` helper and adds `forge: ForgeSyncOutcome` to `RunSyncResult`; `rebuild` stays forge-free (its disposable DB re-syncs on the next `iroha sync`). **Work Graph model — corrected against the schema, which was discovered by a FK failure during testing, not assumed.** Every typed table's primary key FK-references `entities(id)` (`work_items.id`/`pull_requests.id`/`review_comments.id REFERENCES entities(id) ON DELETE CASCADE`), so the `entities` row is the parent and the typed row is the 1:1 detail — the entity must be inserted **first**. This also means a **review comment is a `review` entity** (its `cmt_` id is an entity id): the initial design's "review comments are table-only, not entities" was impossible and was replaced by creating an `entity(type=review)` per comment. So a sync creates: `entity(issue)`+`work_item`, `entity(pull_request)`+`pull_request`, and `entity(review)`+`review_comment` per comment — all at `authority = 80` (design.md §7 verified Git/Forge), `source_kind = github`, `source_ref = url`, `status = "active"`. Ids are stable across syncs via lookup-or-mint by `external_id` (reusing the existing row's id so the shared entity is updated, not duplicated — the added `getReviewCommentByExternalId` gives review comments the same by-external-id lookup issues/PRs already had). Actors are deduped within a run by an in-memory cache over `getActorByProviderExternalId`; email is not fetched so `actors.email_hash` stays null. Relations (all `source_kind = "api"`, confidence 1): `PR ADDRESSES Issue` from `closingIssuesReferences` — inserted only when the issue's entity already exists (FK-safe; a cross-repo or unsynced issue is skipped, issues are persisted before PRs so same-run links resolve) — and `PR REVIEWED_IN review` per comment (connects the review node to its PR in the graph). All writes run in one `withTransaction("write")`; re-sync is idempotent (`upsert*` keep the row id on conflict, `insertRelation` is `ON CONFLICT DO NOTHING`). `@iroha/core` gains `@iroha/forge-github` (§4-allowed) and `zod` deps. Not yet done (PR3): review-learning recurrence candidates and the dashboard/CLI surface. **A fresh-context adversarial review confirmed the five data-integrity invariants hold** (no orphans, idempotency, no cursor regression on failure, no token leakage, no ReDoS in `parseGitHubRef`) and found one HIGH + one MEDIUM, both fixed here: (HIGH) on `truncated`, the watermark had been advanced unconditionally — dropping a large repo's unfetched older tail (>~5k PRs / >~10k issues in a single delta) **permanently and silently** — so on truncation the code now keeps the prior watermark (leaving the gap recoverable by a re-run with a higher page bound) and writes a `sync_required` dirty marker; (MEDIUM) the forge outcome was surfaced nowhere — `iroha sync` now prints a forge line (synced counts / skipped-with-reason / error code / truncation warning), and an enabled-but-tokenless config is reported as `skipped: forge is enabled but <ENV> is not set` (distinct from `disabled`). The success-path `upsertSyncCursor` result is now `.ok`-checked. Deferred with reason: the single large write transaction (consistent with the deferred cross-process-locking stance), refreshing a renamed actor's `display_name`, and doctor's forge token/last-error surfacing (lands with the PR3 surface work); the nested-connection caps (`reviewThreads`/`comments first: N`) are a PR1 provider limitation. Verified: full `pnpm lint`/`typecheck`/`test`/`build` green; `@iroha/core` 135 pass/1 skip (10 `forge-sync` tests: parse/resolve + DB integration for rows/entities/actors/relations/cursor/idempotency/truncation-non-advance/non-fatal-provider-failure), `@iroha/config` 11. | Accepted |
| ID-044 | WP-12 part 3 (`@iroha/core` review-learning recurrence candidates + forge surface) — the last WP-12 slice; completes the P1 GitHub forge provider. **Recurrence detection** (`forge-review-learning.ts`, `detectReviewLearnings(db, repositoryId, threshold, clock, random)`, called at the end of `runForgeSync` after the graph write, **fail-open** — a failure returns 0, never fails the sync): lists **all** the repo's `review_comments` (new `listReviewCommentsByRepository`, a JOIN through `pull_requests` since `review_comments` has no `repository_id`), normalizes each `body_summary` (lowercase; strip fenced/inline code + URLs; collapse whitespace) into a grouping key, and for keys appearing across **≥ `config.forge.review_learning_threshold` DISTINCT pull requests** (a `Set<pull_request_id>`, so an edited/re-synced comment or two comments in one PR can't inflate the count) proposes **one** `review_learning` candidate — pending, human-approved via the existing `approveCandidate`→`build-canonical` path (already handles `review_learning`, unchanged; a candidate is not authoritative until approved). The `KnowledgeProposal` carries `sources[]` = every occurrence's stable `{type:"review", ref:externalId, url}` (provenance), `scope.paths` from the comment paths, and a `forge-recurrence-<sha256-16>` fingerprint **label**; it is `proposalSchema.safeParse`-validated then **`redactProposal`-scanned before storage — fail-closed** (a scanner failure skips the candidate, never stores unscanned external GitHub text). **Idempotent** for sequential runs via `collectExistingRecurrenceLabels` over the new `listCandidatesByType` (all statuses, so approved/rejected learnings also suppress re-proposal); the read-then-write dedup is an **accepted race** for concurrent `iroha sync` (no cross-process lock in v0.1 — same deferred lineage as ID-022/024/026; worst case one duplicate pending proposal). **Config**: `forgeConfigSchema` gains `review_learning_threshold` (`z.number().int().min(2).default(3)` — floor 2 because a "recurrence" of one is a single comment; defaulted so pre-existing configs parse), written by the serializer + `buildDefaultConfig`. **Surface**: `GET /api/v1/sync/status` (`dashboard/sync.ts`) adds a `github` cursor block (lastSuccessAt/lastAttemptAt/lastErrorCode) beside `canonical`; `iroha doctor`'s forge check moved into a new DB-backed `checkForge(cwd)` (config-only `checkProviders` now reports embedding only) that warns when forge is enabled but the token env var is unset (a silent no-op sync would otherwise read as healthy), else reads the `github` cursor to report the last sync's error — token *value* never read (only `!== undefined`) or printed; `iroha sync` prints an `N recurring review learning(s) proposed` line when `reviewLearnings > 0`. **Two fresh-context reviews (security + adversarial) ran; security found no defects** (token non-leak, redaction fail-closed, both new SQL queries fully parameterized, no ReDoS on the 500-char-bounded body, fail-open intact). **Adversarial found five, all resolved here**: (1, MEDIUM) the noise filter was a 16-**UTF-16-unit** floor calibrated for ASCII ("lgtm") that **silently dropped short but meaningful Japanese feedback** ("テストを追加してください" = 12 units) — a first-class-locale regression — so the length floor was **removed** entirely (skip only bodies that normalize to empty, e.g. URL/code-only; triviality is filtered by the distinct-PR threshold + human approval), with a Japanese-recurrence regression test added; (2, LOW) the concurrent-sync dedup race is now an explicit **accepted-race** note in code + here (not a silent gap); (3, LOW) `checkForge` on a fresh clone whose git-tracked `config.yaml` enables forge but whose gitignored `.git/iroha/` DB is not built yet reported a scary `error — "Failed to read sync cursor"`, sending the operator to chase a DB fault instead of running sync → it now **degrades that cursor-read failure to fail-open `ok` "(token set; sync status unavailable — run \`iroha sync\`)"** (genuine DB faults are still surfaced by the storage/guardrail checks that run first); (4, LOW) `checkForge`'s cursor-semantics comment was inaccurate (a success **nulls** lastAttemptAt/lastErrorCode via the unconditional write, it does not leave a "stale lastErrorCode") — corrected, since a future editor trusting it could reintroduce a bug; (5, MINOR) the `.min(1)` threshold floor that allowed nonsensical "recurred across 1 pull requests" is now `.min(2)`. Verified truthfully (not assumed): truncated-then-crosses-threshold is handled (a below-threshold group never enters the dedup set, so it is proposed once the tail arrives), `withTransaction` rolls the whole insert batch back on a mid-loop failure (→ `reviewLearnings: 0`), and the config round-trips. Verified: full `pnpm lint`/`typecheck`/`test`/`build` green (core 142 pass/1 skip incl. 6 new review-learning + 2 doctor-forge tests; `@iroha/config` 11). This completes WP-12; the remaining forge items (GitLab impl, nested-connection pagination, cross-process lock, actor `display_name` refresh) stay deferred with reasons in ID-042/043. **Post-push, the Greptile CI reviewer raised three more, all valid and fixed in a follow-up commit**: (P1) `detectReviewLearnings` runs after the cursor's success write, so an unexpected *throw* (not just an `err`) would flip the completed sync's outcome to `error` (split state: DB shows success, CLI shows error) — the call is now wrapped in a local `try/catch` so the fail-open invariant holds structurally, not by trusting every callee to return a `Result`; (P2) `truncate` sliced on UTF-16 units and could split an emoji surrogate pair into a lone surrogate in the stored title/body — now backs off one unit at a high-surrogate boundary (keeping the `.max()` code-unit contract, unlike a naive code-point spread which would overshoot it), with an emoji-boundary regression test; (P2) `normalizeCommentBody` stripped only ```` ``` ```` fences, letting `~~~`-fenced code create false recurrence groups — the regex now strips both, with a tilde-fence regression test. Verified again: full gate green, core 144 pass/1 skip. | Accepted |
| ID-045 | Dependency vulnerability remediation (`dependency-scan / osv-scan`, `fail-on-vuln: true`) — two advisories newly published in OSV after WP-12 merged, both pre-existing on `main` and unrelated to any feature diff, pinned via `overrides` in `pnpm-workspace.yaml`: `fast-uri` 3.1.3→3.1.4 (GHSA-v2hh-gcrm-f6hx, High; transitive via `ajv`, a semver-compatible patch) and `@hono/node-server` 1.19.14→2.0.10 (GHSA-frvp-7c67-39w9, Medium). **Root cause / safety for `@hono/node-server`**: the direct deps (`@iroha/api`, `@iroha/plugin`) already resolve it to `2.0.10` (patched) via the catalog; the vulnerable `1.19.14` comes **only** transitively from `@modelcontextprotocol/sdk@1.29.0` (its latest), which pins `^1.19.9` and ships no patched 1.x, so an SDK bump cannot fix it. iroha's MCP server uses `StdioServerTransport` (`packages/mcp/src/start.ts`); the SDK's Streamable HTTP transport — the sole `@hono/node-server` consumer — is never imported by any source (grep-confirmed), so the override forces dead-code onto the version the direct deps already use. Verified: vulnerable versions gone from the lockfile, full `pnpm lint`/`typecheck`/`test`/`build` green, `@iroha/mcp` (11) + `@iroha/api` tests pass (SDK still works over stdio); osv-scan turns green. A co-located comment ties the catalog pin to the override (Greptile 4/5 note: a pnpm override wins unconditionally, so a lone catalog bump would be silently ignored). **Accepted residual — the published `@iroha-labs/iroha` package is NOT covered by this override** (a Codex P2 finding on the PR, confirmed against `build-release.ts` + `release.yml`): `buildPublishManifest` resolves only `catalog:`→concrete and emits no `overrides`, and the release runs `npm install` + `npm sbom` in the staging dir, so the SDK's transitive `@hono/node-server` re-resolves to the vulnerable 1.x in the shipped tree/SBOM. This is **not fixed by emitting a manifest override**, because npm honors `overrides` only from the installing *root* project, never from an installed dependency's own manifest — a release-root override would clean the release-build SBOM but misrepresent what end users actually resolve (a misleading attestation), and there is no honest end-user fix while the SDK pins `^1.19.9` with no patched 1.x. **Accepted** because: (a) the code path is unreachable (stdio-only), (b) there is no upstream fix, and (c) the release is human-gated and not yet published (no vulnerable artifact has shipped). Tracked for removal — delete both the override and this note once `@modelcontextprotocol/sdk` ships a release depending on `@hono/node-server` ≥ 2.0.5 (or a patched 1.x). Decision: fix the repo/CI tree honestly, accept-and-track the published-artifact residual, do not fake the SBOM. | Accepted |
| ID-046 | Audit backlog #32 + #46 (`@iroha/api`, `apps/dashboard`) — dashboard search filters wired + the `search/suggestions` stub removed. **#32** (P1): `POST /api/v1/search` was accept-but-ignore — the route parsed `filters` but never forwarded it to `mcpSearch`, the client never sent one, and results were unclickable `<li>`s. The route now forwards `filters`; `searchSchema.filters` is tightened from `z.record(z.string(), z.unknown())` to a `z.strictObject` mirroring the MCP `search` tool's filter set, with `from`/`to` as `z.iso.datetime()` (**stricter** than the MCP tool's plain `z.string()`, because `mcpSearch` compares them lexicographically against `entities.updated_at`, so a non-RFC3339 value would silently mis-window instead of erroring). The client `api.search(query, options)` sends `{mode,limit,filters}` (new `SearchMode`/`SearchFilters`/`SearchOptions`); `Search.tsx` adds entity-type filter chips and wraps each result in a `/knowledge/:id` `<Link>` — every search result is approved knowledge (`search_documents` is populated only from canonical sync, `sync-canonical.ts`), so the detail route is always valid. The chip list is a small dashboard-side const of the 7 knowledge types: the SPA depends only on `@iroha/api`'s type-only exports and cannot import a runtime enum, so a display copy is the architectural boundary, not duplication. The react-query key sorts the selected-type set (`[...types].sort().join(",")`) so click order doesn't fragment the cache. **#46**: removed the always-`[]` `GET /api/v1/search/suggestions` stub (no client/UI/type caller; KISS/YAGNI, and the issue explicitly authorized remove-or-implement) **and updated the contract in the same change** — deleted its row from `dashboard-api.md` §5's endpoint table — resolving the code/spec conflict a spec-compliance review flagged (removing a documented endpoint without updating the doc is a public-API divergence). Reviews addressed: Greptile (order-sensitive query key) + Codex (removed-endpoint contract sync; unvalidated date filters), all fixed here. Integration test seeds+approves a decision (→ searchable) and asserts filter forwarding (`entityTypes:["rule"]` → 0 results, **red on pre-fix** where the filter was dropped), strict unknown-key rejection (400), non-RFC3339 date rejection (400), and 404 for the removed route. Deferred to a follow-up PR (same audit backlog): list-page pagination (#34) and list filters (#35), which touch the GET list routes/pages. Verified: full `pnpm lint`/`typecheck`/`test`/`build` green. | Accepted |
| ID-047 | Audit backlog #19 (P1) + #20 (`@iroha/core`, `@iroha/adapter-codex`) — guardrail enforcement bypasses (security). **#19**: `guard.paths` were matched by the hand-rolled prefix matcher `pathMatches` (`ranking.ts`), which **silently no-op'd standard glob shapes** — `**/*.env`, `src/**/*.ts`, `*.generated.ts` matched nothing, so an author's guard read as active while enforcing nothing (a bypass), and `iroha doctor` reported it healthy. Fixed by giving the guardrail its own matcher `guardPathMatches` backed by **`picomatch`** (added to the catalog + `@iroha/core` deps under `catalogMode: strict`; a pure, ReDoS-resistant, **string-only** matcher — `micromatch`/`fast-glob`/`fs.glob` were rejected because they touch the filesystem and would re-introduce the `..`-before-symlink hazard). The shared `pathMatches` is **left unchanged** for its two search-scope callers (`rankCandidates` boost, `get_active_rules` surfacing) — repurposing it would have altered search semantics; the guardrail's two sites (`evaluateGuardrails`, `guardrailPathViolations`) now use `guardPathMatches`. Options: `{ dot: true }` (mandatory — else a globstar skips a root `.env` / `.github` / `.iroha`) and `{ windows: false }` (POSIX-deterministic regardless of host OS, since targets/paths are always repo-relative POSIX). **Semantics decision (the schema left `guard.paths` as bare `string`, so this defines it):** a guard path protects **the path itself AND its whole subtree** — `guardPathMatches` tests `glob` OR `glob/**`. This was added in response to a **fresh-context `security-diff-reviewer` finding (P1)**: picomatch alone anchors literals, so a bare-directory (`src/generated`) or single-star (`src/*`) guard would match only the literal entry and silently protect nothing under it — **narrower than the pre-picomatch prefix matcher**, a "trading one false-negative for another" regression on a security boundary. The subtree match restores the safe, least-surprising "protect this path and everything under it" behavior while keeping full glob (`**` to cross directories); the `/` boundary is preserved (`foo/**` never matches `foobar/x`). Also: `parseGuardSpec` drops blank/whitespace-only path entries, so a `[""]`/`["  "]` guard is `not_hook_enforceable` in doctor (was falsely `enforceable`) and the blank never reaches picomatch (which throws on an empty pattern — the filter is load-bearing, tested). The `guard.paths` glob contract is now documented in both the Zod schema (`canonical.ts` `.describe()`) and the mirrored `schemas/canonical-v1.schema.json`. **#20**: the codex `apply_patch` parser (`extractApplyPatchTargets`) read only `*** Add/Update/Delete File:` headers and **missed `*** Move to:` rename destinations**, so a move INTO a protected path bypassed the guardrail (Codex-only — every Codex edit is `apply_patch`, while Claude's Edit/Write/MultiEdit are captured separately). Fixed by adding a `*** Move to:` branch that emits the destination as a `write` target and reclassifies the preceding `Update File:` source from `write` to `delete`; a `Move to:` with no preceding write still surfaces the destination write (fail-safe), and since `isProtectedMutation` treats `write`/`delete` identically the reclassify can never flip a denial. Reviewer confirmed clean: no enforcement sibling left on the loose matcher, path-safety invariant intact (no `path.*` calls; inputs pre-canonicalized by `resolveTargets`→`safeRealpath`), Move-to reclassify sound, ReDoS not attacker-reachable (glob is human-approved canonical data; the attacker controls only the target, matched as a literal). All new tests are red on pre-fix code (the glob shapes, the bare-dir/single-star subtree, the blank-path classification, the Move-to rename). **CI review addressed**: Codex (P1) — picomatch was added only to `@iroha/core`, but the published `@iroha-labs/iroha` manifest is generated solely from `packages/plugin/package.json` deps (`build-release.ts` `buildPublishManifest`) with npm deps kept **external** (not bundled), so a clean `npm i @iroha-labs/iroha` would miss picomatch and the bundled hook would fail with `ERR_MODULE_NOT_FOUND` — the same works-in-dev/broken-in-publish class as ID-040 (the `release-pack` test passed only because the monorepo hoists picomatch into the symlinked node_modules). Fixed by declaring `picomatch` in `packages/plugin/package.json` and pinning it with a `release-pack.test.ts` assertion that the assembled publish manifest declares it; a broader manifest-completeness check (every external import in the bundle ⊆ declared deps) is a noted follow-up test-infra gap. Greptile (P2) — added a test pinning the `Move to:`-with-no-preceding-write fail-safe. Verified: full `pnpm lint`/`typecheck`/`test`/`build` + `pnpm test:package` green. | Accepted |
| ID-048 | Audit backlog #28 + #29 (`@iroha/core` `sync-canonical.ts`, `@iroha/storage` `graph-search.ts`) — two canonical→DB sync correctness fixes (both code-only, no migration). **#28 (stale relation prune)**: relation import was insert-only (`insertRelation` is `ON CONFLICT DO NOTHING`), so an edge removed from a document's `relations[]` survived a plain `iroha sync` until a full `--rebuild`. Fixed with **delete-then-insert per document**: `insertCanonicalDocumentRelations` now first calls the new `deleteCanonicalRelationsFromEntity(db, fromEntityId)` (`DELETE FROM relations WHERE from_entity_id = ? AND source_kind = 'canonical'`) then re-inserts the current set. **Scoping** (verified by a storage unit test): the prune keys on `from_entity_id` + `source_kind='canonical'`, so `api`/`git`/`inferred`/`human`/`hook` edges, every *other* document's canonical edges, and any *incoming* edge to this entity are untouched (canonical edges carry `source_ref = NULL`, so `source_ref` is unusable for scoping — `from_entity_id` is unique per canonical document, which is the scoping key). **Atomicity**: delete-then-insert is more dangerous than insert-only (a crash between the two loses the doc's edges), so it must be transactional. The **approve** path already wraps `importCanonicalDocument` + `insertCanonicalDocumentRelations` in one `withTransaction` (unchanged). The **sync** path (`syncCanonicalToDatabase`) previously ran per-document relation inserts non-atomically; it now wraps **each** per-document `insertCanonicalDocumentRelations` call in its own `withTransaction(db, "write", …)` (its callers always pass an open `Database`, never a nested transaction, so no nesting). Idempotent: an unchanged doc is not in the `changed` set, so no prune runs; a re-synced doc prunes then re-creates the same edges. **#29 (authority tiering)**: all canonical docs were hardcoded to `authority = 100` regardless of `frontmatter.status`, so a `superseded`/`archived` doc tied current knowledge in ranking. `sync-canonical.ts`'s `CANONICAL_AUTHORITY = 100` constant is replaced by `authorityForStatus(status)` applied to **both** the entity and the `search_document` rows (the `minimumAuthority` filter/boost reads `entities.authority`; FTS ordering + the hybrid multiplier read `search_documents.authority` — both must move together). **Tier decision (spec-unspecified — database-schema.md §6 documents only "approved canonical = 100"; recorded here per the CLAUDE.md decision rule; it changes MCP ranking, a borderline public surface, but not canonical data)**: `approved = 100`, `superseded = 70`, `archived = 60`. Both non-approved tiers stay **at or above** the `DEFAULT_MINIMUM_AUTHORITY = 60` floor and rank lower only via the missing 80-99/100 boost — NOT by dropping below the floor. (An earlier draft used `superseded = 60`/`archived = 40` to *exclude* archived from default search, but a Codex review flagged that a below-floor status is filtered only **after** the top-N FTS/vector candidate cap: a burst of archived matches could fill the candidate set and starve default search of visible approved/superseded knowledge. Keeping every status ≥ floor avoids that candidate-starvation entirely while still tiering rank.) The `status` enum has exactly three values (`canonical.ts:95`), so the `switch` is exhaustive. The approve path only ever writes `status: "approved"`, so it stays at 100 and the approve/rebuild byte-identical-rows invariant holds. **Accept-and-track (Codex flagged; consistent with the disposable-index model)**: both fixes key on the hash-diff (`added`/`changed` docs), so an **existing** database upgraded in place keeps its old authority-100 rows / stale edges for a doc whose hash has not changed until it is edited or `iroha sync --rebuild` is run — the sanctioned way to re-derive the disposable index. The relation reconcile runs in its own per-document transaction separate from the entity import because the two-pass structure (all entities first, then all relations) is required for cross-document relation *targets* to resolve; a mid-failure there rolls back to the doc's prior edges (no corruption) and is precluded in practice by the single-writer assumption (no cross-process lock — the recurring v0.1 deferral). A fully-retryable per-document relation cursor is out of scope. All new tests are red on pre-fix code (the pruned canonical edge, the surviving human/incoming edges, the superseded/archived authority tiers). Verified: full `pnpm lint`/`typecheck`/`test`/`build` green. | Accepted |
| ID-049 | Audit backlog #21 (`@iroha/git` `run-git.ts`) — close the git-config-resolution env leak, but **scope-locked to extending the denylist, not replacing it with an allowlist**. **The gap**: `runGit` clears git env vars through a denylist (`ALWAYS_STRIPPED_ENV_VARS`); `GIT_CONFIG_GLOBAL`/`GIT_CONFIG_SYSTEM`/`GIT_CONFIG_NOSYSTEM` were absent, so an inherited one made Git read a config file chosen by the ambient parent environment (reproduced: `GIT_CONFIG_GLOBAL=<malformed file> git rev-parse --show-toplevel` fails reading it; cleared, it succeeds). **Fix**: a new `GIT_CONFIG_RESOLUTION_ENV_VARS = [GIT_CONFIG_GLOBAL, GIT_CONFIG_SYSTEM, GIT_CONFIG_NOSYSTEM, XDG_CONFIG_HOME]` group added to the strip list (`GIT_CONFIG`/`GIT_CONFIG_PARAMETERS`/`GIT_CONFIG_COUNT` were already covered; `GIT_CONFIG_KEY_<n>`/`VALUE_<n>` are inert without the already-stripped `GIT_CONFIG_COUNT`). **`XDG_CONFIG_HOME` was added after the fresh-context `security-diff-reviewer` (self-review Step 7) flagged it as a same-class sibling left open** — reproduced: with `GIT_CONFIG_GLOBAL` cleared but an ambient `XDG_CONFIG_HOME` pointing at a dir whose `git/config` is malformed, `git rev-parse --show-toplevel` **still fails** (Git reads its global config from `$XDG_CONFIG_HOME/git/config`), i.e. stripping only `GIT_CONFIG_GLOBAL` is bypassable via the XDG pivot. Stripping it is safe because `HOME` is kept, so Git falls back to the real `$HOME/.config/git/config`. This is exactly the "local optimization leaves a sibling of the same class open" anti-pattern the self-review skill targets; a dedicated red-on-pre-fix integration test now guards it. **Why not the allowlist the issue and `.claude/rules/secure-subprocess-and-credentials.md` prefer (spec/reality conflict surfaced to the human, who chose this scope)**: issue #21's stated verification path was "validate the Windows env set on the windows-2025 CI leg, Tier 1 for this package" — **that premise is false**. `windows-2025` was removed from CI's `verify` matrix and Windows moved to Tier 2 (`compatibility.md` §6; ID-026(14)); `ci.yml`'s matrix is macos-14 / ubuntu-22.04 / ubuntu-22.04-arm only. An allowlist's failure mode is the mirror of a denylist's: it silently **drops** a variable git-for-Windows needs (e.g. an OS-runtime var beyond `SystemRoot`), breaking Git on Windows — and that risk lands **only on Windows**, which cannot be validated on CI or reproduced locally (macOS). Per `windows-ci-compat.md` / `path-and-symlink-safety.md` ("don't treat can't-reproduce-locally as no-problem"), shipping that unvalidated was rejected. The denylist extension only **removes** variables, so it carries zero Windows-breakage risk and closes the named gap. **Accepted structural residual**: the denylist still cannot strip a config env var Git may add in a future release (the exact structural flaw the security rule warns about); the allowlist migration is deferred until Windows is back in a Tier-1 CI matrix that can validate it. Stale run-git.ts / test comments referencing "windows-2025 ... Tier 1 CI matrix" are pre-existing (WP-02-era) and left untouched to keep this security-package diff surgical; flagged for a future doc pass. **Tests** (all red on pre-fix code): a new `run-git.test.ts` integration test asserts an inherited malformed-`GIT_CONFIG_GLOBAL` no longer fails `rev-parse`; the `stripKnownDangerousEnvVars` unit test now includes the three vars (one lowercase, proving case-insensitive stripping). The pre-existing stderr-absolute-path-redaction test's trigger was migrated from `GIT_CONFIG_GLOBAL` (now stripped, so it could no longer fire) to `git config --file <malformed abs path> --list`, which reliably emits the exact absolute path in stderr and survives env-stripping (a repo-local malformed `.git/config` was rejected — reproduction showed Git prints it as a **relative** path, which would make the redaction assertion vacuous). **Accepted directional behavior changes (reviewer Finding 2, Low; consistent with the package's existing GIT_DIR/GIT_CEILING hardening posture)**: (a) stripping `GIT_CONFIG_GLOBAL` also discards a parent-supplied `safe.directory` allow-entry, so in a cross-UID/container checkout where tooling exports `GIT_CONFIG_GLOBAL` to whitelist the repo, `rev-parse` could newly fail with "dubious ownership" — does not affect the primary same-UID own-repo case, and the deterministic-config goal is worth it; (b) stripping `GIT_CONFIG_NOSYSTEM` returns Git to its default of reading `/etc/gitconfig` even if a parent opted out — the one member whose stripping points toward *more* ambient config, but reading system config is Git's default and poisoning `/etc/gitconfig` already requires root, so the net effect is deterministic (ambient-env-independent) resolution, which is the intent. Verified: `pnpm --filter @iroha/git test` 141 green; full `pnpm lint`/`typecheck`/`test`/`build` green. | Accepted |

| ID-050 | Audit backlog #22 + #43 (`@iroha/core` `redact.ts`/`create-checkpoint.ts`, `@iroha/canonical` `secret-scan.ts`) — one PR (both are the pre-storage secret-handling subsystem; P3 defense-in-depth for the local disposable DB, not canonical/committed data). **#22 (complete the free-text field enumeration)**: candidate/proposal redaction skipped several unconstrained free-text fields while the docstring wrongly asserted they "cannot carry a credential". A `relativePath` field rejects only absolute/drive/`..` values and still accepts a credential-shaped substring (`config/x-https://u:tok@h/y`, verified), so it is free-text for this purpose. Now scanned: `proposal.scope.paths`, `proposal.guard.paths`, `proposal.relations[].type`/`.target` (issue-listed) **plus two same-class fields a complete enumeration turned up that the issue did not list — `reference.path` (covers both `sources[].path` and `references[].path`, in `redactReference`) and `implementation[].file` (in `redactCheckpoint`)**. The docstring was corrected to enumerate exactly what is/ isn't scanned, with each "safe" field backed by its actual regex (no unverified safety claim). **Considered and deliberately excluded** (recorded so a reviewer need not re-derive it): `sessionToken` (regex-constrained `ist_<43>`, hashed to `hmac-sha256` and never persisted as plaintext — an auth input, not content) and `idempotencyKey` (`[A-Za-z0-9._:-]{16,128}` — a client dedup key, not content; its charset cannot express an AWS key `/+` or a private-key block, and scanning it risks breaking dedup on a false positive). Every remaining unscanned field is genuinely constrained (`labels`, `scope.languages`, enums, numbers, typed ids). **#43 (secretlint vs gitleaks entropy gap, human-chosen resolution)**: the runtime scanner (`secret-scan.ts`) uses only `@secretlint/secretlint-rule-preset-recommend` (pattern-based, no entropy rule), so a bare high-entropy secret with no known prefix passes it — while CI/pre-commit `gitleaks` has an entropy rule (`generic-api-key`). The same scanner gates canonical writes (reject) and checkpoint redaction (blank), so a **blanket entropy rule was rejected** (its false-positive rate would reject legitimate canonical writes — a real operability cost the issue flagged as med-risk). Instead, per the "prefer a targeted allowlist of known shapes over blanket entropy" option, a `@secretlint/secretlint-rule-pattern` entry was added for the one concrete in-scope high-value token: iroha's own session token `/ist_[A-Za-z0-9_-]{43}/`, and the entropy coverage boundary is documented inline (`secret-scan.ts` comment). New dependency `@secretlint/secretlint-rule-pattern@13.0.2` (same secretlint monorepo/version as the existing packages; existence + integrity verified; added to the pnpm catalog). **Empirically verified** against the real `@secretlint/node` engine: the config detects an `ist_<43>` token (bare, embedded, and inside a path), leaves clean content and a 42-char near-miss untouched, and `maskSecrets` keeps the raw token out of the finding message. **Tests** (all red on pre-fix code — verified by reverting): new `redact.test.ts` unit-tests each newly-scanned proposal/reference field (private-key secret, independent of the `ist_` rule); a `create-checkpoint.test.ts` case redacts an `ist_` token in `implementation[].file` end-to-end (requires both #22 and #43); `secret-scan.test.ts` gains an `ist_`-detected case and a too-short near-miss. **Note** (cross-package test resolution): `@iroha/core` resolves `@iroha/canonical` to its built `dist` (package `exports`), so a `secret-scan.ts` change is only visible to core's tests after canonical is rebuilt — `turbo test` handles this via `dependsOn: ["^build"]`; a filtered `pnpm --filter @iroha/core test` against a stale canonical dist will falsely fail the `ist_`-in-file case. Verified: full `pnpm lint`/`typecheck`/`test`/`build` green. | Accepted |

## Change protocol

Accepted decisions require:

1. a new ADR in `../design.md`;
2. affected machine schema/migration change;
3. backward-compatibility and migration statement;
4. updated acceptance tests;
5. explicit product-owner approval when canonical data, privacy, public API, or distribution changes.

Implementation convenience alone is not sufficient reason to replace an accepted decision.

